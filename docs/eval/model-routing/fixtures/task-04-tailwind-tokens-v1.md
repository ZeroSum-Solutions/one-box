# Task 04 v1: responsive Tailwind v4 component tokens

Produce a compact implementation proposal containing `tokens.css` and one React
`ServiceCard` component. Use Tailwind v4 `@theme` semantic tokens for surface,
text, muted text, border, brand, focus, spacing, radius, and shadow. The component
must have a 44px minimum interactive target, visible keyboard focus, no arbitrary
color values, and a single-column-to-three-column responsive container example.

Output each file in a labeled fenced block. Do not include tests, scores, model
identity, provider identity, or claims that the result passes.
