export const FIRECRAWL_CONSENT_LABEL =
  "Allow paid Firecrawl discovery and fallback";

export const FIRECRAWL_CONSENT_HELP =
  "May incur metered cost for competitor web search. For known URLs, Firecrawl remains a fallback only after the local crawler fails, hits a bot wall, or cannot provide the required format.";
