# Outside grade

## Per-dimension grades

| # | Dimension | Grade | Evidence (one sentence, cite a selector, file, or screenshot region) |
|---|-----------|-------|----------------------------------------------------------------------|
| 1 | Hero distinctiveness | B+ | The desktop hero layers image-specific annotation, alternating title alignment, proof chips, and staged copy, but `.hero__callout` disappears below 860px and leaves mobile with a conventional text-over-photo stack. |
| 2 | Color character | B | `site.css` establishes warm canvas, tinted, and dark surface tiers with disciplined orange accents, although the beige/ink/orange palette remains attractive rather than unmistakably WITS-specific. |
| 3 | Motion design | A- | `site.js` and `.reveal` provide letter-level hero choreography, staggered intersection reveals, moving hover states, a pulsing callout, shared timing tokens, and complete reduced-motion handling. |
| 4 | Section division & rhythm | B+ | The desktop capture alternates tinted panels, dark bands, timelines, full-bleed imagery, and offset media, but `.about` becomes cavernous on desktop and the mobile gallery-to-about sequence is visually prolonged. |
| 5 | Card craft | B | `.scope-card` adds a clipped corner, index, lift, shadow, and arrow reveal while `.audience-card` and `.coverage-card` provide variants, but several still resolve to bordered boxes and “More on this” is a non-interactive `<span>`. |
| 6 | Visual hierarchy | B+ | Consistent `.eyebrow`, committed Fraunces display type, restrained body copy, and strong dark/light inversions create clear reading order, though secondary copy becomes very small and recessive in the 1440px capture. |
| 7 | Variety / anti-monotony | A- | The page moves through card grids, a split manifesto, handover ledger, seam grid, timeline, photo strip, asymmetric portrait, coverage cards, and accordion without repeating consecutive topologies. |
| 8 | Balance & composition | B | The hero and `.receive__grid` distribute weight effectively, but the sparse process row and tall narrow `.about__panel` leave conspicuous unused space on desktop while mobile stacks several large image blocks consecutively. |
| 9 | Bespoke-ness | B | Cable-test annotation, labeled-handover narrative, technical imagery, and 15/30-year proof make this cabling-specific, but generic stock photography and unsupported operational copy prevent it from feeling authentically specific to WITS. |

## Overall grade

B

This is clearly above the D benchmark and contains a real visual and motion system, but its strongest art direction is desktop-only, several secondary sections fall back to polished template patterns, and the bespoke impression depends heavily on stock imagery and claims the brief never supplied.

## Ranked fixes

1. **Replace all stock photographs with commissioned WITS work and pin annotations to genuinely documented installation details.**  
   Hero distinctiveness B+ to A-, bespoke-ness B to A-, and trustworthiness materially improves.

2. **Remove or verify every invented licensing, insurance, minimum-job, occupied-building, testing, documentation, and service-policy claim.**  
   Bespoke-ness B to B+ by replacing synthetic specificity with defensible company proof; it also removes the launch-blocking trust failure.

3. **Retain an image-specific signature composition on mobile instead of hiding `.hero__callout` and reverting to a stacked panel.**  
   Hero distinctiveness B+ to A- and balance B to B+.

4. **Recompose `.about__grid` around a wider contextual image, owner portrait, or documented project artifact rather than the tall stock rack crop.**  
   Balance B to A- and bespoke-ness B to B+.

5. **Turn each `.scope-card` into a real destination or remove the false “More on this” affordance, then add cable-specific diagrams or deliverable previews.**  
   Card craft B to A- and bespoke-ness B to B+.

6. **Animate section internals independently—rows, image panels, timeline connectors, and captions—instead of revealing whole sections as one `.reveal` block.**  
   Motion design A- to A by extending the existing vocabulary into deliberate section choreography.

7. **Tighten the sparse desktop process/about spacing and break the mobile run of full-width imagery with captions, offsets, or horizontal scrolling.**  
   Section division B+ to A- and balance B to A-.

8. **Bind the palette to verified logo colors and apply one repeatable technical surface treatment across annotations, cards, and data panels.**  
   Color character B to A- and card craft B to B+.

## Trust check

The page ships fictional contact details—`(407) 555-0142` and `estimates@witscabling.example`—despite acknowledging them only in a source comment. `index.html` also invents Florida low-voltage licensing and insurance/COI status, “no fixed minimum,” occupied-building and after-hours practices, routine interstate travel, same-person continuity, and universal testing, certification, labeling, test-result, and as-built handover policies. Finally, all six images are stock photographs explicitly marked “must not ship” in `img/ATTRIBUTION.md`, yet visible copy such as “The work,” “On this cable,” and “Fiber termination at the panel” presents them as evidence of WITS’s own work.

Reading additional input from stdin...
OpenAI Codex v0.146.1
--------
workdir: /Users/zero-suminc./projects/one-box/spikes/wits-variants/c-artdirected/site
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 01a00713-9c4d-7032-8833-cca3442fc3e8
--------
user
You are an outside auditor grading a generated marketing website. You did not build it; the builder's own checks all pass, and your job is to find where the site is nonetheless mediocre. Be adversarial: the recurring failure mode you exist to catch is a builder over-grading its own work. Do not grade on a curve and do not award credit for intent — grade only behavior that actually ships and renders.

Your working directory contains the site's full source (HTML/CSS/JS). Read it — motion design especially must be audited from the code, because the attached screenshots cannot show animation. The two attached images are full-page captures at 1440px (desktop) and 390px (mobile).

THE CLIENT BRIEF the site was generated from:

<brief>
# Standard test brief — WITS

This is the **canonical intake** for comparing build avenues. Every avenue
(Refero-only baseline, current pipeline, layout-IR, future variants) must be
given exactly this brief, with nothing added and nothing removed, so the
outputs are comparable.

Version: 1. Frozen 2026-08-15. Do not edit — fork to `BRIEF-v2.md` if it changes.

---

## Business Overview

- **Business name:** Wiggins Information Technology Services (WITS)
- **Business description:** Structured cabling and technology infrastructure
  installation services.
- **In business:** 15 years
- **Industry experience:** 30 years
- **Primary service area:** Orlando, Florida area
- **Service areas:** Colorado and Florida
- **Certifications:** none
- **Key differentiators:**
  - Quality workmanship
  - Attention to detail
  - Reliability
  - Personal service
  - Customer satisfaction

## Services & Priorities

Primary priority:
- Network structured cabling
- Fiber optic cabling
- IT room design

Also offered:
- Data network cabling
- Access point installation
- Camera systems
- Audio/visual cabling
- Troubleshooting

No services need to be phased out or downplayed.

## Target Audience

- Commercial businesses
- Offices
- New construction projects

Customers needing: reliable network infrastructure, new office/network
installations, IT room design, fiber installation, cabling upgrades,
troubleshooting and infrastructure problem-solving.

## Visual Direction

- Existing logo and brand colors are available; files provided separately.
- Open to either a light or dark website background.
- No specific competitor/inspiration websites.
- Desired feel: professional, modern, trustworthy, clean, technical,
  dependable, high-quality, approachable.

## Existing Content

- No existing project photos or videos.
- Stock imagery and/or new professional project photography will be needed.
- No existing bios, testimonials, reviews, or case studies.
- No existing website or old copy to reuse.
- All website content must be developed from scratch.

## Call to Action

- **Primary CTA:** Call for an estimate.
- Feature both phone and email prominently.
- Keep the contact process simple.
- No online booking/scheduling system.

## FAQs

Should address: services offered, project timelines, troubleshooting.
No special policies or requirements to highlight upfront.

## Logistics

- No existing domain name; domain selection needed.
- No predetermined additional pages.
- Designer recommends the site structure/pages.
- No hard deadline or launch date.

## Additional Notes

- Customer satisfaction is an important priority.
- The site should communicate WITS as an experienced, reliable, detail-oriented
  commercial technology installation company.
- **Primary purpose:** establish trust and get commercial customers to call for
  an estimate.

---

## Constraints that bind every avenue

1. No invented certifications, awards, licenses, or memberships.
2. No invented testimonials, client names, review counts, or star ratings.
3. No invented project photography presented as WITS's own work.
4. Numbers that may be stated: 15 years in business, 30 years industry
   experience. Nothing else.

</brief>

THE RUBRIC (the human judge of record wrote the criticisms it encodes; his grades anchor the scale — a site matching the baseline failures it describes is a D):

<rubric>
# Presentation rubric — the bar every generated site is graded against

Created 2026-08-15 from Devin's grading of the WITS refero-baseline spike
(commit `314da19`, served at the spike's `site/` directory). This rubric is
separate from the frozen Phase-4 A/B rubric (`rubric.md`) — that one compared
pipeline arms; this one grades a single site's presentation quality against
Devin's standard.

## Authority

- **Devin is the judge of record.** A build's grade is his grade. Model grades
  (self-grades or outside audits) are advisory input to his verdict, never a
  substitute for it.
- **Every build gets an outside-model audit** — a model from a different vendor
  than the builder (current lane: GPT-5.6 Sol via `codex exec`) grades against
  this rubric before the build reaches Devin. Precedent: the 2026-08-14 GPT-5.6
  audit caught real WCAG failures and invented policies the builder reported as
  clean.
- Engine gates (contrast, axe, token drift, …) are **floors, not grades**. The
  WITS baseline passes every gate and still grades D. A gate can prove a site
  is not broken; only this rubric measures whether it is good.

## The benchmark: WITS baseline = D

Devin's verdict, 2026-08-15, verbatim criticisms → gradeable dimensions below.
"The information on it is pretty good but its presentation is a D." A generated
site that would tie this baseline fails. The measured facts behind the grade:
the site's entire CSS contains **1 animation and 6 transitions**; hover
feedback exists on exactly two element classes (CTA buttons and nav links,
both a bare color swap); consecutive sections are divided by nothing except
one orange band.

## Dimensions (grade each A–F, then overall)

### 1. Hero distinctiveness
*Baseline failure: "the hero's generic."*
- F–D: headline + subhead + button + photo in a default arrangement any
  template could produce.
- B: a deliberate entrance sequence (staged reveals), a composition that
  responds to the imagery, at least one element that could only belong to this
  company.
- A: the hero alone tells you who the company is; layered composition
  (typography, imagery, motion, background treatment) with a clear focal path.

### 2. Color character
*Baseline failure: "the colors are generic."*
- F–D: one brand hue + white/gray applied flatly; accent used identically
  everywhere it appears.
- B: a palette with structure — surface tiers, warm/cool tension or tonal
  depth, accent deployed by rule (interactive-only, or emphasis-only), at
  least one section that shifts the scheme (dark band, tinted band).
- A: the palette itself is recognizably the company's; gradients/tints/dark
  sections orchestrated so no two adjacent sections read as the same surface.

### 3. Motion design
*Baseline failure: "there is no scrolling animations… only a couple hover
animations… which just literally changes the color from black to orange."*
- F–D: static page; hover = color swap only.
- B: scroll-triggered reveals with stagger on grouped items; hover states that
  move (lift, scale, glow, icon slide) with tuned duration/easing; a hero
  entrance. Motion respects `prefers-reduced-motion`.
- A: a motion *system* — consistent easing/duration vocabulary, choreographed
  section entrances, at least one signature moment (marquee, parallax,
  count-up, sticky transition) that fits the brand.

### 4. Section division & rhythm
*Baseline failure: "the sections are not at all well divided, the
information's kind of squished together… between sections we only have this
one orange break."*
- F–D: uniform white sections separated by padding alone; one divider device
  reused (or none).
- B: alternating surface treatments (tint, dark band, contained card-panel),
  generous and *varied* vertical rhythm, each section boundary legible at a
  glance while scrolling.
- A: the page has an arc — sections sequence like scenes (open, build,
  contrast, close), division devices varied and deliberate (bleed images,
  band inversions, offset panels), never twice the same trick in a row.

### 5. Card craft
*Baseline failure: "there aren't any cool cards… they're just information in
a white box."*
- F–D: white box, border, text stacked inside.
- B: cards with anatomy — icon/number treatment, layered background (tint,
  gradient edge, glow), hover behavior (lift/tilt/reveal), internal hierarchy
  (eyebrow, title, body, action) that reads in order.
- A: card design is a signature of the site — a treatment you'd screenshot;
  variants for different content types instead of one card for everything.

### 6. Visual hierarchy
*Baseline failure: "there's no visual hierarchy with information."*
- F–D: headings step down in size but everything carries equal visual weight;
  nothing tells the eye where to go first.
- B: strong display-to-body contrast (scale, weight, color), eyebrow labels,
  clear primary/secondary/tertiary reading order in every section, one focal
  point per viewport.
- A: hierarchy survives the squint test at every scroll position — blur the
  page and the structure of what matters still shows.

### 7. Variety / anti-monotony
*Baseline failure: "too much monotony within the site."*
- F–D: every section is the same layout (centered heading → grid of boxes)
  with different words.
- B: layout topology varies across the page — asymmetric splits, offset
  media, full-bleed moments, list vs. grid vs. feature alternation.
- A: no two consecutive sections share a topology, yet the page still reads
  as one designed system, not a collage.

### 8. Balance & composition
*Baseline: "the balance isn't super bad, but I don't think it's ideal."*
- F–D: visibly lopsided sections, orphaned elements, cramped or cavernous
  gaps.
- B: weight distributed deliberately within each section; whitespace used as
  a design element, not leftover space.
- A: compositions feel inevitable — nothing could move without getting worse.

### 9. Bespoke-ness (the summary dimension)
*Baseline failure: "as far as it being like a bespoke website for this
individual company, I'd lower my grade and say this is more like a D."*
- F–D: swap the logo and copy and this could be any company in any industry.
- B: industry-specific composition choices (imagery treatment, section
  choices, motifs) plus company-specific ones (their palette, their voice,
  their proof points made visual).
- A: the site could not be mistaken for a competitor's. Content, imagery,
  layout and motion all argue for *this* company.

## Reference standard

The motionsites template catalog (`references/motionsites/`, cloned from
github.com/vikod3 with Devin's authorization, 2026-08-15) is the working
definition of the bar: **Devin judged every template in that catalog better
than the WITS baseline.** Template-derived craft standards are appended below
as they are extracted. License note: these repos are grading references only —
their code is never shipped into client sites.

### Template-derived standards (extracted 2026-08-15, 9 templates analyzed)

Full per-template reports: `template-craft-notes.md`. The cross-template
findings, ranked by how consistently the catalog uses them:

1. **A signature-move economy, not blanket polish.** Every template earns its
   premium feel from 2–4 deliberate devices; several (serein, forge, neon,
   dental) ship *zero* scroll reveals and still beat our baseline. Grade the
   presence and quality of signature moves, not a checklist of effects.
2. **Video-as-life** (7 of 9): full-bleed autoplay muted loop video,
   *disciplined into the brand* — desaturated (`saturate-0`) and re-tinted via
   `mix-blend-mode: color` / `color-dodge` / multiply overlays so footage
   becomes an on-brand texture, not a colorful stock clip. This one device
   supplies most of the kinetic energy on most of these sites.
3. **One surface formula, fractally reused.** A single glass/card recipe
   (translucent fill + backdrop-blur + white hairline border + inset or soft
   shadow — e.g. glow's `rgba(116,116,116,0.07)` + `border-[1.5px]
   rgba(255,255,255,0.40)` + `blur(50px)` + `rounded-[32px]`) applied to
   feature cards, testimonial cards, badges, and icon chips alike. Cohesion
   comes from reuse; cheapness comes from one-off effects.
4. **Eyebrow systems.** A consistent pre-headline device on every section:
   forge/neon's `DM Mono uppercase 0.48px-tracked text-white/50` label,
   radiant's icon+pill badge. The strongest, cheapest hierarchy move in the
   whole catalog.
5. **Display type that commits.** 88–120px display against 12–18px body;
   often `font-normal` (restraint over boldness); negative tracking scaled to
   size (glow: −2.5px display → −1.5px H2 → −0.5px H3 → 0 body); two-family
   pairing (Space Grotesk + DM Mono, Jakarta + Manrope). Our baseline's
   headings "step down politely" — these commit.
6. **Accent-by-rule.** Near-monochrome palettes with ONE scoped accent moment:
   nexora's single gradient headline word; glow's red on interactive elements
   only, never headings; dental's amber on the five rating stars only. The
   rule is legible — the viewer can infer where accent will appear next.
7. **Entrance choreography, hand-tuned.** Where motion exists it is asymmetric
   and deliberate: nexora's cascade at delays 0 / 0.8 / 1.0 / 1.2s with
   letter-level stagger (0.07s/char) on the headline; lumen's
   `staggerChildren: 0.2, delayChildren: 0.3` with `whileInView` +
   `viewport={{once: true, margin: '-100px'}}`; glow's text-settles-first,
   image-blooms-at-+0.5s. Uniform simultaneous fade-ins (serein) read as
   scaffold defaults.
8. **Hover that moves.** Scale (1.02–1.05), shadow lift, icon reveal, gradient
   text fill — 200–300ms. Color-swap-only hover is the baseline's failure
   mode and the catalog's floor.
9. **Division by rhythm and surface, not walls.** Huge, *varied* vertical
   padding (`py-[100px]`→`py-[200px]`), 5%-opacity hairline rules, footer as
   a rounded-top card on a gradient, hairline-seam grids
   (`gap-px` over a dark backdrop) instead of boxed cards. Uniform `py-24`
   everywhere (serein) is the tell of a scaffold.
10. **Layered, art-directed composition.** radiant's image-with-offset-glass-
    panel motif (reused 5×); dental's product-annotation callout pinned by a
    JS-positioned connector line to a specific tooth in the photo, and copy
    right-aligned to interlock with the model's face. Composition responding
    to the *specific* imagery is the essence of bespoke.

**Anti-lesson:** the catalog is full of dead craft — wired-but-unused stagger
systems, animation component libraries no page imports. Grade only shipped,
rendering behavior; never credit intent found in the code.

## How the outside grader uses this

Inputs per build: full-page desktop + mobile screenshots, the brief, the
site's full source (screenshots cannot show motion — motion is audited from
code), and this rubric. Output: per-dimension letter grades with one-sentence
evidence each, an overall letter, and a ranked list of the highest-leverage
fixes. The report lands in the run's evidence directory next to the gate
reports.

</rubric>

Respond with EXACTLY this structure and nothing else:

# Outside grade

## Per-dimension grades

| # | Dimension | Grade | Evidence (one sentence, cite a selector, file, or screenshot region) |
|---|-----------|-------|----------------------------------------------------------------------|
(one row per rubric dimension, 1-9)

## Overall grade

A single letter (A-F, +/- allowed) on its own line, then one short paragraph of justification.

## Ranked fixes

The 5-10 highest-leverage changes, most impactful first. Each: one line naming the change, one line saying which dimension(s) it lifts and roughly how far. Concrete, not aspirational — "add a scroll-reveal system with staggered entrances per section (Motion: F to C+)" not "improve animations".

## Trust check

Anything in the site that is dishonest, invented, or inconsistent with the brief (claims, policies, numbers, imagery implying things the brief does not support). Say "none found" if clean.
2026-08-15T20:18:44.489017Z ERROR codex_rmcp_client::oauth::refresh_transaction: error=failed to refresh OAuth tokens for server Vercel: OAuth token refresh failed: Server returned error response: invalid_grant: Refresh token is invalid.
warning: Skill descriptions were shortened to fit the 2% skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.
codex
I’m auditing the rendered composition against the source, with special attention to shipped motion, responsive behavior, and unsupported claims.
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '1,240p' ~/AGENTS.md && rg -n -i \"one-box|wits|artdirected|presentation rubric\" /Users/zero-suminc./.codex/memories/MEMORY.md | head -80 && pwd && git status --short && rg --files -g '"'!node_modules'"' -g '"'!.next'"' | sed -n '1,240p'" in /Users/zero-suminc./projects/one-box/spikes/wits-variants/c-artdirected/site
 succeeded in 0ms:
# AGENTS.md — shared conventions for ALL agents on this machine

> The ONE cross-tool conventions file. **Codex, Cursor, Antigravity, Hermes** read this
> natively. **Claude Code** reads `~/.claude/CLAUDE.md` (kept consistent with this file);
> do NOT symlink over it. Per-project `AGENTS.md` at a repo root OVERRIDES this for that
> project and adds Commands / Stack / Structure. Keep this human-curated and short.
>
> Machine: Apple M4 Max, 48 GB, macOS 26. Operator: subscription-first solo engineer.

## What this machine is
A unified multi-agent coding environment for shipping high-quality **websites, web
apps, agentic workflows, and automations**. Multiple IDEs/agents (Claude Code, Codex/
GPT-5.6, Gemini CLI, Cursor, Antigravity, Hermes) operate in **lanes**, unified by ONE
shared memory bus + these conventions. Tools rotate (48 GB is real) — context follows
you via memory, not by keeping everything hot.

## Billing — HARD RULES (a violation is a P0 bug)
<!-- SYNC:billing v2026-08-07 (prohibitions mirrored in ~/.claude/CLAUDE.md billing bullet; the vendor roster is NOT mirrored — both sides point at ~/.claude/billing-lanes.md, which is the single source. Bump BOTH stamps only when a PROHIBITION changes.) -->
- **Subscription-first for programmatic/agent LLM calls, plus an explicit OK list.**
  **Lane order: OAuth/subscription FIRST, always** — Claude = Max OAuth; Codex CLI
  (ChatGPT/Codex OAuth, runs GPT-5.6 — sol for code/creation, luna for light calls,
  terra ~never; gpt-5.5 retired 2026-07-09); Gemini via Antigravity CLI (`agy`, keyring
  auth); Ollama = local. Go to a metered API only when no subscription lane can serve the
  task, or the subscription lane is slow/saturated. **Direct billed `api.anthropic.com`
  is forbidden.** **A vendor not on the approved OK list needs explicit approval before
  the first call** — never assume, never "just try it". **Image generation: Higgsfield
  FIRST** (subscription credits — Devin wants it used frequently), then Kie.ai, then
  FAL/others.
  → The full OK list — every approved vendor with its key, host, billing caveats, and the
  OpenRouter premium roster — is **`~/.claude/billing-lanes.md`**. **Read that file before
  using any metered API.**
- **Cursor + Antigravity are subscription GUI editors, NOT a programmatic API lane.**
  Their plans can incur credit overage → set a HARD spend cap, keep them human-driven,
  NEVER place them in an unattended/agent loop. This is distinct from the per-token rule
  above (which governs scripted LLM calls); it is not a license for metered agent traffic.
- **Forbidden by default** for programmatic calls: any LLM API host not on the OK list
  above (e.g. billed api.anthropic.com, api.openai.com direct). Allowed per-token hosts:
  `api.deepseek.com`, `api.moonshot.ai`, `api.firecrawl.dev`, `openrouter.ai`, `api.kie.ai`, `fal.run`,
  `inference-api.nousresearch.com`, `generativelanguage.googleapis.com` (Gemini API,
  Ultra credit-funded); non-LLM SaaS host: `api.smith.langchain.com`
  (LangSmith, observability only).

## Scrape routing — HARD RULE
<!-- SYNC:scrape-routing v2026-07-16 (mirror of ~/.claude/CLAUDE.md scrape-routing bullets; bump BOTH stamps when either side changes) -->
- **crawl4ai FIRST, Firecrawl as BACKUP (added 2026-07-16).** `crawl4ai` v0.9.0 is
  installed locally (uv tool venv, headless Chromium, `crwl` on PATH) and is **free and
  uncapped**. Fetching a known URL goes through it first. Canonical wrapper:
  `~/projects/tools/zs-skills/skills/research/deep-dive/scripts/crawl4ai-scrape.sh <url> <out_dir>`
  — provenance header, bot-challenge detection, `ERR` + exit 1 so the caller falls back.
- **Firecrawl (`firecrawl_scrape`) costs credits and is the SECOND tier**, reached only
  when: crawl4ai returns `ERR`/bot-challenge; the target is behind a wall crawl4ai can't
  pass (Cloudflare/anti-bot, paywall, login); or the task needs a Firecrawl-only format
  (`branding`). Known-bot-walled surfaces (Google Maps, Yelp, BBB) may go straight to
  Firecrawl. Firecrawl ERR → `WebFetch`.
- **This OVERRIDES the Firecrawl MCP server's own instructions**, which tell every session
  to use Firecrawl as the primary web tool. That text auto-ships in context on all seven
  surfaces, predates the local install, and does not know crawl4ai exists. Ignore it.
- **Search is a separate axis — crawl4ai cannot search.** `crwl` takes a URL; it is a
  scraper, not a search engine. For discovery prefer subscription-billed Gemini (`agy`,
  or `deep-dive/scripts/gemini-search.sh`) or built-in `WebSearch`. `firecrawl_search` is
  OK-listed and permitted, but never the reflex.

## Secrets — HARD RULES
<!-- SYNC:secrets v2026-07-27 (mirror of ~/.claude/CLAUDE.md op/ZS-Vault bullet; bump BOTH stamps when either side changes) -->
- **Never** call `op` / 1Password CLI *autonomously*. **ZS Vault** is the source of truth:
  env vars from `~/.config/zs-api-keys.env`, or `zsvault get <id>` for one-shot reads.
  Add new creds with `zsvault add <id>` — never hand-edit `zs-api-keys.env`.
  Only when the user *explicitly asks* for `op` this session: `ZS_ALLOW_OP=1 op …`
  (opt-in token, never self-applied — same semantics as the Claude Code hook).
- **Never** commit secrets, an age key, or `zs-api-keys.env` to any repo.

## Browser & sign-in — HARD RULES
<!-- SYNC:browser-signin v2026-07-23 (mirror of ~/.claude/CLAUDE.md browser/sign-in bullets; bump BOTH stamps when either side changes) -->
- **Never open a blank/ephemeral browser profile** (Devin's "hard no", 2026-07-22). Web tasks
  needing his context use his REAL Chrome (`chrome-control` MCP — wired into Cursor, Codex,
  Hermes, Antigravity, and Claude Code as of 2026-07-23; claude-in-chrome for Claude surfaces)
  or `open <url>` — never a fresh Playwright/Chromium profile with no sessions, 1Password, or
  bookmarks. Anonymous localhost/dev-server checks are the only exception.
- **Sign in FOR Devin before asking him.** Default account for EVERYTHING:
  **devszerosum@gmail.com** — pick it first at every account picker/SSO screen; it lives in
  Chrome's **Default profile**, which is the profile agents drive. NotebookLM/Gemini Notebook
  now uses that same default account. The remaining legacy exception is Notion workspace →
  "Devin Wiggins PT" — use the data-holding account there and flag the mismatch. Passwords are in ZS Vault for HIS reference; at a real password field, use
  1Password Agentic Autofill (Claude surfaces only) or stop for Devin — never type a vault
  password/TOTP into browser automation.
- **CLI OAuth playbook (proven 2026-07-23, Higgsfield):** when a CLI wants to open a browser
  for auth, suppress it (`BROWSER=/usr/bin/true <cli> auth login`), capture the printed URL,
  and open that URL in Devin's real Chrome via the `chrome-control` MCP (`open_url`). His live
  sessions usually reduce the whole flow to a single consent click — no password screen at all.

## Supabase — HARD RULES (org selection)
<!-- SYNC:supabase v2026-07-08 (mirror of ~/.claude/CLAUDE.md §Supabase; bump BOTH stamps when either side changes) -->
- **PRIMARY org: `Zerosumsolutions-Projects`** (slug `zljkaiwwkbpeyjsblwyb`, Pro). Wire every
  new Supabase project here and check here FIRST — all production / client work lives here.
- **LEGACY/secondary: `rise-scheduling`** (slug `vpzwurcxuliwzumdinwn`, free). Don't start new
  work here. Its `offeafzjrkwqavrhwvqg` ("wiggdevin's Project", INACTIVE) is a stale duplicate —
  never use (left in place, not deleted).
- **Auth:** ZS Vault `SUPABASE_ACCESS_TOKEN` is a Management-API token that sees ALL orgs
  (`api.supabase.com` for cross-org ops). Verify reach without printing secrets:
  `zs-credential-access` → `scripts/verify_supabase_org.py <org_slug>`.
- The Claude.ai Supabase **MCP connector** is OAuth-scoped to `Zerosumsolutions-Projects` only
  (reconnected 2026-06-27); for anything it can't reach, use the Management API + vault token.

## Memory — shared zs-memory bus (the crux)
- Endpoint: `http://localhost:8765/mcp` (MCP streamable HTTP) or `/api/v1/memories/`
  (REST, trailing slash required). **Bearer auth on everything except `/health`** —
  since 2026-07-10 every tool uses its OWN scoped token via the shared wrapper
  `~/bin/mem0-mcp.sh <client>` (vault ids `zs_memory_token_<client>`; scoped tokens
  cannot delete — deletes go through the vault-held `zs_memory_maintenance_token`,
  human-approved). Legacy shared `zs_memory_token` remains valid pending rotation.
  Provenance via `X-Client-Name` header; namespace via `X-User-Id` header (default
  `zero-fleet`). Full wiring snippets + runbook:
  `~/.claude/reference_mem0_openmemory_setup.md` (the canonical doc).
- **Namespacing:** global/cross-project facts → `user_id=zero-fleet`. Project isolation →
  `user_id=zero-fleet__<project>` (the ONLY isolation boundary; reads span all tools
  within a user_id, never across projects).
- **Store DECISIONS, not CODE.** ✅ decisions, conventions, preferences, project-state,
  gotchas. 🚫 code/diffs/file contents, secrets, transient task state, anything
  re-derivable from the repo (agentic grep beats RAG — Boris Cherny).
- **Only persist a DURABLE cross-session fact** (decision / convention / preference /
  project-state / gotcha) — NOT transient work-in-progress (use the task tracker for
  that; "project-state" = a settled milestone like "Phase 2 bus live", not "currently
  editing X"). Every agent routes writes through its memory-write gate (Claude Code: the
  `mem0-write-protocol` skill; other tools: the equivalent wrapper) — NEVER
  `mem0_add_memories` raw. Enforcement does NOT depend on client cooperation: the bus has
  a SERVER-SIDE write-policy guard that rejects code/secrets for ALL clients, dedup-on-write
  (cosine ≥ 0.90 → rejected with the existing id), plus a daily consolidation cron that
  flags drift. One fact per memory, phrased for a future agent with no context.
- **Writing is not optional for wired tools.** Cursor and Antigravity are wired to the bus
  but historically wrote nothing (2026-07-09 audit) — settled decisions made in ANY tool
  must land on the bus before the session ends, or they are invisible to the rest of the
  fleet. Session scratch, private brains (`~/.gemini/antigravity/brain/`), and chat logs
  stay tool-local; the DECISION goes to mem0.

## Second brain — Google NotebookLM (ALL tools)
- NotebookLM is the durable RESEARCH-CONTENT store (the second brain): all substantial
  research output for any subject — briefs, papers, scraped findings, competitive intel —
  gets added there as sources; query it with cited RAG before re-researching a topic.
  Complements the memory bus: mem0 = decisions/preferences/state, NotebookLM = content.
- Access: local `notebooklm` CLI (`~/bin/notebooklm`, notebooklm-py) — CLI-first, never
  browser automation. Usage, autonomy rules, and the routing convention live in the
  shared `notebooklm` skill (`zs-skills skills/research/notebooklm`, synced to all tools).
  The **only supported local profile** is `devszerosum` / `devszerosum@gmail.com`.
  Default sink: owned Second Brain `ba7ffa61-849a-42cc-93cb-b429aa133d3e`; pass
  `-p devszerosum` explicitly for writes and never create an additional local profile.
- Auth = Devin's Google session cookies (profile `devszerosum` / devszerosum@gmail.com). On auth
  errors run `~/bin/notebooklm-signin`: it auto-harvests a LIVE work session from any local
  browser/profile (chrome profiles, brave, safari) — usually NO login needed — then falls back
  to the desktop app, then a browser, all fail-closed. In UNATTENDED/headless contexts use
  `~/bin/notebooklm-refresh` (same multi-source harvest, no windows) and report-and-stop if it
  can't recover. Weekly `com.zs.notebooklm-refresh` keeps the jar warm. Never type Google
  credentials; the NotebookLM account is always devszerosum@gmail.com.

## Deliverable Routing (ALL tools — Codex, Cursor, Hermes, Gemini, Antigravity)
<!-- SYNC:deliverable-routing v2026-08-01 (mirror of ~/.claude/CLAUDE.md §Deliverable Routing; bump BOTH stamps when either side changes) -->
- Every AI-generated deliverable goes to `~/Inbox/<type>/` unless it belongs inside a
  specific project tree: single-file HTML / visual briefs → `~/Inbox/html/` · plan docs →
  `~/Inbox/plans/` · generated PDFs → `~/Inbox/pdfs/` · screenshots / QA evidence →
  `~/Inbox/screenshots/` · loose markdown notes / specs → `~/Inbox/notes/` · anything
  else → `~/Inbox/misc/`. Sanctioned subdirs: `notes/handoffs/` (session handoffs),
  `plans/<slug>/` (multi-file plan bundles), `deletions/` (staging for reviewed
  deletions, created by the 2026-07-05 inbox audit — empty only on Devin's explicit say-so).

| Deliverable | Destination |
|---|---|
| Goal-run machine state (not a deliverable) | `~/.claude/goal-state/<slug>/` |
| Intake (machine-intake watcher input) | `~/Inbox/intake/` |
| **Backup / snapshot / dump (not a deliverable)** | **`~/Backups/<service>/`** |
- **Backups NEVER go in `~/Inbox/`** (added 2026-08-01). `~/Inbox/` is deliverables, and the
  weekly `inbox_sweep` ages everything in it out to `_archive/` — wrong for machine state you
  may need to restore from. Volume archives, DB dumps, config snapshots, and pre-change
  backups go to `~/Backups/<service>/` (`colima/`, `supabase/`, `mishmash/`, `mcp-config/`,
  `claude-desktop/`, plus the existing `rise-scheduler/`, `zs-mem0-backups/`,
  `project-ledger/`). Give each a retention rule where it's written — house pattern is
  keep-last-N, see `~/bin/rise-db-backup.sh`.
- **Codex:** do NOT leave artifacts in `~/Documents/Codex/` — outputs worth keeping go to
  `~/Inbox/<type>/`; session scratch is disposable.
- Project-scoped artifacts → the project tree (e.g. `<project>/docs/screenshots/`).

## Shared skills — zs-skills write protocol (ALL tools)
> Canonical store: `~/projects/tools/zs-skills/` (git-backed). Tool skill dirs
> (`~/.claude/skills`, `~/.codex/skills`, `~/.gemini/skills`, `~/.cursor/skills`,
> `~/.hermes/skills/shared`) hold SYMLINKS into it. Improve skills through the
> symlinks under these rules; never fork a private copy.
- **Patch-on-use:** when a shared skill proves wrong or stale during use, patch it
  immediately — targeted, minimal edits only.
- **Evidence required:** every patch cites an observed failure (error output, wrong
  result, changed external behavior) in its commit message. No vibes-based rewrites.
- **Never wholesale-rewrite** a skill or the store; **never delete** — move to
  `.archive/` instead.
- **Creating:** new skills → `skills/<category>/<name>/SKILL.md` with the superset
  frontmatter (store `CLAUDE.md` has the schema), `lane: experimental`,
  `author: <your-tool-name>`; must pass `~/projects/tools/zs-skills/bin/zs-skills lint`.
  Experimental skills sync only to their creating tool until the curator promotes them.
- **Commit every write** (conventional style). Uncommitted writes are swept nightly
  through a scan gate (snyk-agent-scan + lint); flagged writes are git-reverted and
  reported.
- **Never `git commit --amend` to fix your own commit here.** This store has concurrent
  writers (the nightly sweep, other agent sessions). They advance HEAD between your commit
  and your amend, so `--amend` silently rewrites *their* commit instead of yours. Make a
  new commit. Enforced on this machine: an amend must carry `ZS_AMEND_HEAD=<literal-sha>`
  that still matches HEAD, or the hook refuses it.
- **Banned in any skill** (lint-enforced): `allowed-tools` frontmatter,
  dynamic-context `` !`cmd` `` execution, runtime remote fetches.

## ~/projects taxonomy
Top level = active products only. `clients/<Name>/` = client work incl. client-specific
tools. `services/` = running daemons — NEVER reorganize (launchd references these paths).
`tools/` = own tooling; `tools/third-party/` = upstream reference clones. `plugins/` =
Claude Code plugins. `archive/` = retired projects (move there, don't delete). One-sprint
research/planning doc-dirs are NOT projects — they go to `~/Inbox/notes/` or `~/Inbox/plans/`.

## Code style (defaults; per-project AGENTS.md overrides)
- Immutability (return new objects, never mutate inputs). Validate at boundaries
  (Zod/Pydantic). Handle errors explicitly — no silent swallows.
- KISS / DRY / YAGNI. Small files (<400 lines), small functions (<50), nesting ≤4.
- Naming: `camelCase` vars/fns, `PascalCase` types/components, `UPPER_SNAKE` consts,
  `use*` hooks, booleans `is/has/should/can`.
- **Web design tokens only — NO raw Tailwind literals.** Tailwind v4 OKLCH `@theme`,
  60/30/10, ÷8 spacing, brand fonts (NOT Inter), shadcn/ui + CVA. Run the anti-slop gate.

## Boundaries
- ✅ always: read/grep the repo; run fast gates (biome, `tsc --noEmit`); write durable
  decisions to memory through the write-policy.
- ⚠️ ask first: schema changes, new deps, deploys, deleting files, destructive memory ops
  (`delete_all_memories` requires an explicit human instruction).
- 🚫 never: hardcode secrets; `op read`; force-push; route Claude/Codex via per-token
  APIs; write live code into the memory bus; put Cursor/Antigravity in an unattended loop.
- Destructive git (`branch -D`, force-push, `reset --hard`, `clean -f`, `checkout .`) only
  when the human explicitly asked for it this session (2026-07-07 rule; Claude Code enforces
  via hook + `ZS_ALLOW_GIT=1`, other tools apply it as policy).

## Model lanes
- **Sonnet 5** = agentic/subagent default (skip Opus-class for routine code); **Opus 5**
  (+ fast mode) is the default driver for both everyday interactive work and complex
  agentic coding — it replaced Opus 4.8 in both roles on 2026-07-24. **Fable 5** is
  reserved for genuinely long-horizon autonomous runs, maximum-capability calls, and
  highly ambiguous architecture (matches `~/.claude/CLAUDE.md` subagent rule and the
  `ops/model-lane-picker` skill).
- **Opus 5 effort starts at `high`** (the API default), NOT `xhigh` — this inverts the
212:# Task Group: Fable 5 prompt-rewriting, One-Box, and merge-safe Mishmash planning
214:scope: rewrite user-provided text into execution-ready Fable 5 prompts for reconstruction, One-Box, research, and merge-safe Mishmash planning; preserve prompt-only boundaries and distinguish supplied policy/context from verified project state
217:## Task 1: Rewrite graph-based website reconstruction and One-Box workflow
225:- Fable 5, prompt rewriting, ROLE, OBJECTIVE, DELIVERABLES, CONSTRAINTS, OUTPUT FORMAT, One-Box, View mode, Edit mode, Crawl4AI, Firecrawl, GSAP, Tailwind v4, design tokens
280:- One-Box prompts specify direct text/button editing, View/Edit modes, a collapsible workbench rail, upload/research intake, Website/Web app/iOS targets, Tailwind v4 semantic CSS variables/design tokens, and direct visual/function verification. Use Crawl4AI first; Firecrawl only after failure, bot challenge, unsupported capability, or approved paid fallback, recording provider/failure/URL/timestamp/confidence. GSAP uses a validated serialized motion model, reduced-motion support, and cleanup; never arbitrary JavaScript execution. [Task 1]
/Users/zero-suminc./projects/one-box/spikes/wits-variants/c-artdirected/site
?? ../../../refero-baseline/audit/grade-desktop.png
?? ../../../refero-baseline/audit/grade-mobile.png
?? ../../../refero-baseline/audit/outside-grade-log.txt
?? ../../../refero-baseline/audit/outside-grade-raw.md
site.js
site.css
index.html

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "wc -l index.html site.css site.js && sed -n '1,260p' index.html && sed -n '1,320p' site.js" in /Users/zero-suminc./projects/one-box/spikes/wits-variants/c-artdirected/site
 succeeded in 0ms:
     526 index.html
     747 site.css
      92 site.js
    1365 total
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>WITS — Structured Cabling &amp; Technology Infrastructure | Orlando, FL</title>
<meta name="description" content="Wiggins Information Technology Services installs structured cabling, fiber, and IT rooms for commercial buildings in the Orlando area. 30 years in the field. Call for an estimate.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600;9..144,700&amp;family=Inter:wght@400;500;600;700;800&amp;display=swap" rel="stylesheet">
<link rel="stylesheet" href="site.css">
<!--
  Variant C — "Art-Directed Light". Content and facts are copied verbatim
  from spikes/refero-baseline/site/index.html. Presentation only is new.
  PLACEHOLDERS — replace before launch. See baseline CLAIMS.md for the register.
    Phone   (407) 555-0142        — 555-01XX is the reserved fiction range
    Email   …@witscabling.example — .example is a reserved TLD
    Photos  Unsplash stock, img/ATTRIBUTION.md — NOT WITS's own work.
-->
</head>
<body>

<a class="skip-link" href="#main">Skip to content</a>

<header class="site-header">
  <div class="shell site-header__inner">
    <a class="brand" href="#top">
      <span class="brand__mark">WITS</span>
      <span class="brand__sub">Wiggins Information Technology Services</span>
    </a>
    <nav class="primary-nav" aria-label="Primary">
      <a href="#scope">Scope</a>
      <a href="#audience">Who for</a>
      <a href="#receive">What you get</a>
      <a href="#process">Process</a>
      <a href="#faq">FAQ</a>
    </nav>
    <a class="header-call" href="tel:+14075550142">(407) 555-0142</a>
  </div>
</header>

<main id="main">

  <!-- Hero · full-bleed photograph art-directed against the tester in frame;
       annotation callout pinned to the VeeX MTX150's live test screen. -->
  <section class="hero" id="top">
    <div class="hero__frame shell">
      <div class="hero__media">
        <img src="img/hero.jpg" width="1400" height="1050" alt="A fiber tester connected to a network switch during commissioning." loading="eager">
        <div class="hero__scrim" aria-hidden="true"></div>

        <div class="hero__copy">
          <p class="eyebrow">Structured cabling &middot; Orlando, Florida</p>
          <h1 class="hero__title" id="hero-title">
            <span class="line line--l">Your building&rsquo;s</span>
            <span class="line line--r"><em class="accent-word">cabling</em> should be</span>
            <span class="line line--l">the last thing</span>
            <span class="line line--r">you worry about.</span>
          </h1>
          <p class="hero__sub">
            WITS designs and installs structured cabling, fiber, and IT rooms
            for commercial buildings around Orlando. Thirty years in the
            field, fifteen running this business, and every run labeled and
            documented so the next person to open the closet can read it.
          </p>
          <div class="hero__actions">
            <a class="btn btn-primary" href="tel:+14075550142">Call for an estimate</a>
            <a class="btn btn-ghost" href="mailto:estimates@witscabling.example">Email us</a>
          </div>
          <div class="hero__proof">
            <span>15 years in business</span>
            <span>30 years in the field</span>
            <span>Orlando &middot; FL &middot; CO</span>
          </div>
          <p class="hero__caption">
            <strong>Pictured:</strong> every fiber run is tested and
            certified before it&rsquo;s handed over &mdash; not just pulled
            and hoped for.
          </p>
        </div>

        <div class="hero__callout" aria-hidden="true">
          <svg class="hero__callout-line" viewBox="0 0 100 100" preserveAspectRatio="none">
            <line x1="71" y1="80" x2="78" y2="55"></line>
          </svg>
          <span class="hero__callout-dot"></span>
          <div class="hero__tip">
            <span class="hero__tip-label">On this cable</span>
            <p>Every fiber run is tested and certified before it&rsquo;s handed over.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Scope · three priorities as die-cut cards, the rest as a dense index -->
  <section class="shell scope" id="scope">
    <div class="head reveal">
      <p class="eyebrow">Scope of work</p>
      <h2>Three things we <span class="accent-word--ink">lead with</span>.</h2>
      <p class="lede">
        These are the jobs WITS is built around. The rest of the list is part
        of the same work &mdash; it just isn&rsquo;t what you should call us
        first for.
      </p>
    </div>

    <div class="scope__grid">
      <article class="scope-card reveal" style="--i:0">
        <p class="scope-card__index">01</p>
        <h3>Network structured cabling</h3>
        <p>
          Copper backbone and horizontal runs for offices, suites, and full
          floors. Every drop labeled at both ends and recorded on a plan you
          keep.
        </p>
        <span class="scope-card__arrow">More on this &rarr;</span>
      </article>
      <article class="scope-card reveal" style="--i:1">
        <p class="scope-card__index">02</p>
        <h3>Fiber optic cabling</h3>
        <p>
          Single-mode and multimode between closets, floors, and buildings.
          Terminated, tested, and handed over together with the results.
        </p>
        <span class="scope-card__arrow">More on this &rarr;</span>
      </article>
      <article class="scope-card reveal" style="--i:2">
        <p class="scope-card__index">03</p>
        <h3>IT room design</h3>
        <p>
          Racks, pathways, grounding, and cable management laid out before
          anything gets pulled &mdash; so the room is still workable in five
          years.
        </p>
        <span class="scope-card__arrow">More on this &rarr;</span>
      </article>
    </div>

    <div class="scope__also">
      <p class="eyebrow">Also installed</p>
      <ul class="scope__list">
        <li>Data network cabling</li>
        <li>Access point installation</li>
        <li>Camera systems</li>
        <li>Audio / visual cabling</li>
        <li>Troubleshooting</li>
      </ul>
    </div>
  </section>

  <!-- Who this is for · icon-chip cards, tinted band shifts the surface -->
  <section class="audience" id="audience">
    <div class="shell">
      <div class="head reveal">
        <p class="eyebrow">Who this is for</p>
        <h2>Four ways people call us.</h2>
      </div>
      <div class="audience__grid">
        <article class="audience-card reveal" style="--i:0">
          <span class="audience-card__chip" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 21h18M6 21V9l6-5 6 5v12M9 21v-6h6v6"/></svg>
          </span>
          <h3>New construction</h3>
          <p>Cabling designed into the build and coordinated with the general contractor&rsquo;s schedule, rather than squeezed in at the end.</p>
        </article>
        <article class="audience-card reveal" style="--i:1">
          <span class="audience-card__chip" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="7" width="18" height="13" rx="1"/><path d="M8 7V4h8v3"/></svg>
          </span>
          <h3>Office fit-outs and moves</h3>
          <p>New suites, floor changes, and relocations &mdash; including work in buildings that stay occupied while it happens.</p>
        </article>
        <article class="audience-card reveal" style="--i:2">
          <span class="audience-card__chip" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4v16M4 6h13l-2 3 2 3H4"/></svg>
          </span>
          <h3>Cabling upgrades</h3>
          <p>Older plant brought up to what the network actually needs now, without tearing out what still works.</p>
        </article>
        <article class="audience-card reveal" style="--i:3">
          <span class="audience-card__chip" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.5 2"/></svg>
          </span>
          <h3>Something is down</h3>
          <p>Faults traced, bad runs re-terminated, and undocumented installs mapped so the next person is not guessing.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- Accent statement band · full-bleed ink, colour shift -->
  <section class="creed reveal">
    <div class="shell creed__grid">
      <div>
        <p class="eyebrow eyebrow--on-ink">The difference</p>
        <p class="creed__statement">
          You should be able to open the closet in five years and still know
          what every cable does.
        </p>
      </div>
      <div>
        <hr class="creed__rule">
        <p class="creed__note">
          Quality workmanship, attention to detail and reliability are what
          every contractor says. What they mean here is labeling at both
          ends, test results you are handed, and a record of the plant that
          outlives whoever installed it.
        </p>
      </div>
    </div>
  </section>

  <!-- What you receive · contained offset panel -->
  <section class="shell receive reveal" id="receive">
    <div class="head">
      <p class="eyebrow">What you receive</p>
      <h2>What lands on your <span class="accent-word--ink">desk</span> at handover.</h2>
    </div>
    <div class="receive__grid">
      <div class="receive__list">
        <div class="receive-row">
          <span class="receive-row__n">01</span>
          <div><h3>Labeled plant</h3><p>Every run identified at both ends, matching the record.</p></div>
        </div>
        <div class="receive-row">
          <span class="receive-row__n">02</span>
          <div><h3>Test results</h3><p>Certification results for the runs installed, handed over with the job.</p></div>
        </div>
        <div class="receive-row">
          <span class="receive-row__n">03</span>
          <div><h3>An as-built record</h3><p>A plan of what was installed and where it terminates, that you keep.</p></div>
        </div>
        <div class="receive-row">
          <span class="receive-row__n">04</span>
          <div><h3>One person to call</h3><p>The same contact through quoting, install and anything after.</p></div>
        </div>
      </div>
      <div class="receive__panel">
        <div class="receive__panel-offset" aria-hidden="true"></div>
        <figure>
          <img src="img/fiber.jpg" width="1400" height="937" alt="A fiber patch panel with terminated and labelled cassettes." loading="lazy">
        </figure>
        <figcaption>Fiber termination at the panel</figcaption>
      </div>
    </div>
  </section>

  <!-- Before we start · technical console ledger, inverted -->
  <section class="quals reveal" id="quals">
    <div class="shell">
      <div class="head">
        <p class="eyebrow eyebrow--on-ink">Before we start</p>
        <h2>Working with GCs and property managers.</h2>
        <p class="lede">
          Commercial jobs have paperwork before they have cable. These are
          the things a general contractor or building manager usually asks
          for, and they get sorted before anyone is on site.
        </p>
      </div>
/**
 * WITS — variant C behaviour.
 * 1. Letter-level stagger on the hero headline (JS-injected spans).
 * 2. IntersectionObserver scroll reveals, once, staggered via --i.
 * Both guarded by prefers-reduced-motion.
 */
(function () {
  "use strict";

  var reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  /* ---- Hero headline letter stagger ------------------------------------ */

  var title = document.getElementById("hero-title");

  if (title) {
    if (reduceMotion) {
      title.classList.add("is-static");
    } else {
      var startDelay = 0.12; // seconds, after the eyebrow badge
      var step = 0.055; // seconds per character, within the 0.05-0.07s band
      var lineGap = 0.3; // seconds added per line, so lines cascade rather
      // than queue behind one another char-by-char (this headline runs 4
      // lines long — a single continuous counter would push the last line
      // out past 2.5s, which reads as sluggish rather than staggered).

      function wrapChars(node, base) {
        var count = 0;
        var children = Array.prototype.slice.call(node.childNodes);
        children.forEach(function (child) {
          if (child.nodeType === Node.TEXT_NODE) {
            var frag = document.createDocumentFragment();
            child.textContent.split("").forEach(function (ch) {
              if (ch === " ") {
                frag.appendChild(document.createTextNode(" "));
                return;
              }
              var span = document.createElement("span");
              span.className = "ch";
              span.textContent = ch;
              span.style.animationDelay = (base + count * step).toFixed(3) + "s";
              count += 1;
              frag.appendChild(span);
            });
            node.replaceChild(frag, child);
          } else if (child.nodeType === Node.ELEMENT_NODE) {
            if (child.classList && child.classList.contains("accent-word")) {
              // The gradient (background-clip: text) needs a direct text
              // node to clip to — per-letter spans would break it. Fade the
              // whole word in as one unit instead, in the same cascade.
              child.classList.add("word-fade");
              child.style.animationDelay = (base + count * step).toFixed(3) + "s";
              count += child.textContent.length;
              return;
            }
            count += wrapChars(child, base + count * step);
          }
        });
        return count;
      }

      var lines = title.querySelectorAll(".line");
      lines.forEach(function (line, i) {
        wrapChars(line, startDelay + i * lineGap);
      });
      title.classList.add("is-animating");
    }
  }

  /* ---- Scroll reveals ---------------------------------------------------- */

  var revealTargets = document.querySelectorAll(".reveal");

  if (reduceMotion || !("IntersectionObserver" in window)) {
    revealTargets.forEach(function (el) { el.classList.add("is-visible"); });
    return;
  }

  var io = new IntersectionObserver(
    function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          io.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.15, rootMargin: "0px 0px -8% 0px" }
  );

  revealTargets.forEach(function (el) { io.observe(el); });
})();

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '261,560p' index.html && sed -n '1,260p' site.css" in /Users/zero-suminc./projects/one-box/spikes/wits-variants/c-artdirected/site
 succeeded in 0ms:
      <div class="quals__grid">
        <div class="quals-cell">
          <p class="eyebrow eyebrow--on-ink">Insurance</p>
          <p>Certificate of insurance issued to the general contractor or building owner on request.</p>
        </div>
        <div class="quals-cell">
          <p class="eyebrow eyebrow--on-ink">Licensing</p>
          <p>Florida low-voltage licensing details supplied with the estimate.</p>
        </div>
        <div class="quals-cell">
          <p class="eyebrow eyebrow--on-ink">Site rules</p>
          <p>Badging, induction, working hours and building access agreed before mobilisation.</p>
        </div>
        <div class="quals-cell">
          <p class="eyebrow eyebrow--on-ink">Scope sign-off</p>
          <p>Written scope agreed in advance, so variations are visible rather than argued about later.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Process · stepped timeline -->
  <section class="shell process" id="process">
    <div class="head reveal">
      <p class="eyebrow">What to expect</p>
      <h2>How a project runs.</h2>
    </div>
    <div class="steps">
      <article class="step reveal" style="--i:0">
        <p class="step__n">01</p>
        <h3>Call</h3>
        <p>Tell us the site, the scope, and the timeline you&rsquo;re working to.</p>
      </article>
      <article class="step reveal" style="--i:1">
        <p class="step__n">02</p>
        <h3>Walkthrough</h3>
        <p>We look at the space, the pathways, and what is already in the walls before quoting anything.</p>
      </article>
      <article class="step reveal" style="--i:2">
        <p class="step__n">03</p>
        <h3>Written estimate</h3>
        <p>Scope, materials, and schedule in writing, so you can hold it against anything else on your desk.</p>
      </article>
      <article class="step reveal" style="--i:3">
        <p class="step__n">04</p>
        <h3>Install and handover</h3>
        <p>Installed, tested, labeled, documented. You get the records, not just the cable.</p>
      </article>
    </div>
  </section>

  <!-- The work · full-bleed photo band with scrim -->
  <section class="work reveal" id="work">
    <div class="shell">
      <div class="head">
        <p class="eyebrow">The work</p>
        <h2>What this looks like on site.</h2>
      </div>
    </div>
    <div class="work__strip">
      <figure class="work-shot">
        <img src="img/copper.jpg" width="1400" height="933" alt="Copper patch cables dressed into a panel." loading="lazy">
        <figcaption>Copper patching, dressed and identified</figcaption>
      </figure>
      <figure class="work-shot">
        <img src="img/itroom.jpg" width="1400" height="933" alt="An equipment room aisle with racks either side." loading="lazy">
        <figcaption>IT room, laid out before anything is pulled</figcaption>
      </figure>
      <figure class="work-shot">
        <img src="img/office.jpg" width="1400" height="933" alt="A commercial office interior with meeting room and glazing." loading="lazy">
        <figcaption>The environment it all disappears into</figcaption>
      </figure>
    </div>
  </section>

  <!-- Who you're dealing with · offset portrait panel -->
  <section class="shell about reveal" id="about">
    <div class="about__grid">
      <div class="about__panel">
        <div class="about__panel-offset" aria-hidden="true"></div>
        <figure>
          <img src="img/rack.jpg" width="1400" height="2100" alt="Rack detail with dressed cabling." loading="lazy">
        </figure>
      </div>
      <div>
        <p class="eyebrow">Who you&rsquo;re dealing with</p>
        <h2>Thirty years of this, <span class="accent-word--ink">fifteen</span> of it here.</h2>
        <p class="lede">
          WITS is small on purpose. The person who walks your site is the
          person who quotes it and the person you call when something needs
          looking at again. That is what &ldquo;personal service&rdquo; is
          supposed to mean, and it is the reason the work gets documented the
          way it does &mdash; because we are the ones who come back to it.
        </p>
        <p class="lede">
          Fifteen years running this business in Florida and Colorado, thirty
          years in the trade before and through it.
        </p>
      </div>
    </div>
  </section>

  <!-- Coverage · data cards on a tinted band -->
  <section class="coverage reveal" id="coverage">
    <div class="shell">
      <div class="head">
        <p class="eyebrow">Coverage</p>
        <h2>Where we work.</h2>
      </div>
      <div class="coverage__grid">
        <div class="coverage-card">
          <p class="eyebrow eyebrow--on-ink">Service area</p>
          <p class="coverage-card__primary">Orlando, Florida</p>
          <ul>
            <li><span>Orlando metro</span><span>Primary</span></li>
            <li><span>Florida</span><span>Statewide</span></li>
            <li><span>Colorado</span><span>Served</span></li>
          </ul>
        </div>
        <div class="coverage-card">
          <p class="eyebrow eyebrow--on-ink">Who we work with</p>
          <p class="coverage-card__primary">Commercial buildings.</p>
          <ul>
            <li><span>Commercial businesses</span><span>&mdash;</span></li>
            <li><span>Offices and suites</span><span>&mdash;</span></li>
            <li><span>New construction</span><span>&mdash;</span></li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ · accordion list -->
  <section class="shell faq reveal" id="faq">
    <div class="head">
      <p class="eyebrow">Questions</p>
      <h2>Asked often.</h2>
    </div>
    <div class="faq__list">
      <details class="faq-item" open>
        <summary>What does WITS install?
          <span class="faq-item__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></span>
        </summary>
        <p>
          Network structured cabling, fiber optic cabling, and IT room design
          are the core of it. We also handle data network cabling, access
          point installation, camera systems, audio/visual cabling, and
          troubleshooting. If it carries a signal through a commercial
          building, it is probably on the list &mdash; call and ask.
        </p>
      </details>
      <details class="faq-item">
        <summary>How long does a project take?
          <span class="faq-item__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></span>
        </summary>
        <p>
          It depends on the size of the site and how much access we get. A
          single suite is a different job from a floor in an occupied
          building or a new build still on the schedule. You get a schedule
          in writing with the estimate, before any work starts, rather than a
          guess on the phone.
        </p>
      </details>
      <details class="faq-item">
        <summary>Can you troubleshoot cabling someone else installed?
          <span class="faq-item__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></span>
        </summary>
        <p>
          Yes. Tracing faults, re-terminating bad runs, and making sense of
          an undocumented install is regular work here. Thirty years in the
          field mostly means having already seen it.
        </p>
      </details>
      <details class="faq-item">
        <summary>Can you work in an occupied building?
          <span class="faq-item__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></span>
        </summary>
        <p>
          Yes. Occupied floors, live suites and tenanted buildings are normal
          commercial work. Access windows, noise and after-hours requirements
          get agreed before we mobilise, not discovered on day one.
        </p>
      </details>
      <details class="faq-item">
        <summary>Is there a minimum job size?
          <span class="faq-item__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></span>
        </summary>
        <p>
          No fixed minimum. Small jobs and single-drop calls are worth the
          phone call &mdash; we would rather tell you it is a twenty-minute
          fix than have you wait on a quote for it.
        </p>
      </details>
      <details class="faq-item">
        <summary>Do we get test results and a record of what was installed?
          <span class="faq-item__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></span>
        </summary>
        <p>
          Yes. Certification results for the runs installed and a record of
          the plant are part of handover, not an extra. If a previous
          installer left you without either, that is something we can put
          right on its own.
        </p>
      </details>
      <details class="faq-item">
        <summary>Are you insured and licensed?
          <span class="faq-item__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></span>
        </summary>
        <p>
          Yes. A certificate of insurance can be issued to the general
          contractor or building owner on request, and licensing details are
          supplied with the estimate.
        </p>
      </details>
      <details class="faq-item">
        <summary>Do you travel outside Orlando?
          <span class="faq-item__icon" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg></span>
        </summary>
        <p>
          Orlando and Central Florida are the main service area. Work
          elsewhere in Florida and in Colorado is normal &mdash; ask, and we
          will tell you straight whether it makes sense for your job.
        </p>
      </details>
    </div>
  </section>

  <!-- Contact · full-bleed ink band -->
  <section class="contact" id="contact">
    <div class="shell contact__grid">
      <div>
        <h2>Call for an estimate.</h2>
        <p class="contact__lede">
          Tell us what the building needs. We walk the site, then put the
          scope, the schedule, and the price in writing.
        </p>
        <div class="contact__actions">
          <a class="btn btn-on-ink" href="tel:+14075550142">Call (407) 555-0142</a>
        </div>
      </div>
      <div class="contact__lines">
        <div class="contact-line">
          <p class="eyebrow eyebrow--on-ink">Phone</p>
          <a href="tel:+14075550142">(407) 555-0142</a>
        </div>
        <div class="contact-line">
          <p class="eyebrow eyebrow--on-ink">Email</p>
          <a href="mailto:estimates@witscabling.example">estimates@witscabling.example</a>
        </div>
      </div>
    </div>
  </section>

</main>

<footer class="site-footer">
  <div class="shell site-footer__inner">
    <span>Wiggins Information Technology Services</span>
    <span>Structured cabling &middot; Orlando, FL</span>
    <span>Florida &middot; Colorado</span>
  </div>
</footer>

<script src="site.js"></script>
</body>
</html>
/**
 * WITS — variant C "Art-Directed Light"
 *
 * Warm light canvas + one deep ink tone + brand orange under a strict rule:
 * accent appears in exactly three places, everywhere —
 *   1. the eyebrow dot before every section label
 *   2. interactive elements (buttons, links, focus rings)
 *   3. exactly one emphasised word or figure per section
 * Nowhere else. No decorative accent borders, tints, or fills.
 *
 * Fonts: Fraunces (display/serif) + Inter (body/UI). Two families, per the
 * hard constraint. Fluid type/space via clamp(); edge tokens hold layout
 * consistent from 390px to 1440px+.
 */

/* ---- Tokens -------------------------------------------------------------- */

:root {
  /* Colour */
  --ink: #17130f;               /* deep ink — headings, dark bands */
  --ink-2: #332b23;             /* secondary ink — body copy on canvas */
  --ink-muted: #6b5f52;         /* labels, captions — 5.77:1 on canvas */
  --canvas: #faf6ef;            /* warm light page background */
  --canvas-deep: #f1e9db;       /* sunk / tinted band */
  --surface: #fffdf9;           /* card surface, just off canvas */
  --line: #e4dccb;              /* hairline border, warm */
  --line-on-ink: rgba(250, 246, 239, 0.16);

  --accent: #f35b22;            /* WITS orange — interactive + dark-bg emphasis */
  --accent-deep: #be400f;       /* AA-safe orange for text on light — 4.96:1 */
  --accent-soft: #fde3d4;       /* faint tint for icon chips */

  /* Fluid type */
  --fs-display: clamp(40px, 8vw, 112px);
  --fs-h2: clamp(30px, 4.2vw, 54px);
  --fs-h3: clamp(19px, 1.7vw, 23px);
  --fs-lede: clamp(16.5px, 1.3vw, 19px);
  --fs-body: clamp(14.5px, 1vw, 16px);
  --fs-label: 12px;
  --fs-stat: clamp(28px, 3vw, 40px);

  --lh-tight: 0.95;
  --ls-tight: -0.04em;

  /* Fluid space */
  --edge-x: clamp(20px, 3.4vw, 48px);
  --edge-y: clamp(20px, 4.8vh, 48px);
  --band-lg: clamp(96px, 12vw, 168px);
  --band-md: clamp(64px, 8vw, 112px);
  --band-sm: clamp(44px, 6vw, 80px);
  --gap-lg: clamp(28px, 3.4vw, 56px);
  --gap-md: clamp(18px, 2.2vw, 32px);

  --r-card: 20px;
  --r-frame: 28px;
  --r-pill: 999px;

  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --dur-fast: 200ms;
  --dur-med: 300ms;
  --dur-slow: 600ms;

  --max: 1280px;
}

/* ---- Reset ----------------------------------------------------------------- */

*, *::before, *::after { box-sizing: border-box; }
html { -webkit-text-size-adjust: 100%; scroll-behavior: smooth; }
body {
  margin: 0;
  background: var(--canvas);
  color: var(--ink-2);
  font-family: "Inter", system-ui, -apple-system, "Segoe UI", sans-serif;
  font-size: var(--fs-body);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
img { max-width: 100%; display: block; }
h1, h2, h3 { margin: 0; font-family: "Fraunces", Georgia, serif; color: var(--ink); }
p { margin: 0; }
a { color: inherit; }
ul { margin: 0; padding: 0; list-style: none; }
.shell { max-width: var(--max); margin-inline: auto; padding-inline: var(--edge-x); }

:where(a, button, summary):focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 3px;
  border-radius: 4px;
}

.visually-hidden {
  position: absolute; width: 1px; height: 1px; overflow: hidden;
  clip: rect(0 0 0 0); white-space: nowrap; border: 0; padding: 0; margin: -1px;
}

.skip-link {
  position: absolute; left: var(--edge-x); top: -60px;
  background: var(--ink); color: var(--canvas); padding: 12px 18px;
  border-radius: var(--r-pill); z-index: 100; text-decoration: none;
  font-weight: 600; transition: top var(--dur-fast) var(--ease-out);
}
.skip-link:focus { top: var(--edge-y); }

/* ---- Eyebrow — the first accent slot -------------------------------------- */

.eyebrow {
  display: inline-flex; align-items: center; gap: 9px;
  font-size: var(--fs-label); font-weight: 600; letter-spacing: 0.14em;
  text-transform: uppercase; color: var(--ink-muted); margin: 0;
}
.eyebrow::before {
  content: ""; width: 7px; height: 7px; border-radius: 50%;
  background: var(--accent); flex: 0 0 auto;
}
.eyebrow--on-ink { color: rgba(250, 246, 239, 0.72); }

.head {
  display: flex; flex-direction: column; gap: 14px;
  max-width: 58ch; margin-block-end: var(--gap-lg);
}
.head h2 { font-size: var(--fs-h2); line-height: 1.05; letter-spacing: -0.02em; font-weight: 600; }
.head .lede { font-size: var(--fs-lede); color: var(--ink-muted); max-width: 52ch; }

.accent-word {
  background: linear-gradient(100deg, var(--accent-deep), var(--accent) 60%, #ff8a52);
  -webkit-background-clip: text; background-clip: text; color: transparent;
  font-style: normal;
}
.accent-word--ink {
  background: linear-gradient(100deg, var(--accent-deep), var(--accent) 70%);
  -webkit-background-clip: text; background-clip: text; color: transparent;
}
.accent-figure { color: var(--accent-deep); font-weight: 700; }
.accent-figure--on-ink { color: var(--accent); }

/* ---- Buttons — second accent slot ----------------------------------------- */

.btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 8px;
  min-height: 48px; padding: 12px 24px; border-radius: var(--r-pill);
  font-family: "Inter", sans-serif; font-size: 15px; font-weight: 700;
  text-decoration: none; cursor: pointer; border: 1.5px solid transparent;
  transition: transform var(--dur-med) var(--ease-out),
              background-color var(--dur-med) var(--ease-out),
              border-color var(--dur-med) var(--ease-out),
              box-shadow var(--dur-med) var(--ease-out),
              color var(--dur-med) var(--ease-out);
}
.btn-primary {
  background: var(--accent); color: var(--ink); border-color: var(--accent);
  box-shadow: 0 1px 2px rgba(23, 19, 15, 0.12);
}
.btn-primary:hover {
  background: var(--accent-deep); border-color: var(--accent-deep); color: var(--canvas);
  transform: translateY(-2px);
  box-shadow: 0 10px 24px -8px rgba(190, 64, 15, 0.55);
}
.btn-ghost {
  background: transparent; border-color: var(--ink); color: var(--ink);
}
.btn-ghost:hover { background: var(--ink); color: var(--canvas); transform: translateY(-2px); }
.btn-on-ink { background: var(--accent); color: var(--ink); border-color: var(--accent); }
.btn-on-ink:hover { background: var(--canvas); border-color: var(--canvas); color: var(--ink); transform: translateY(-2px); }

/* ---- Header ---------------------------------------------------------------- */

.site-header {
  position: sticky; top: 0; z-index: 50;
  background: color-mix(in srgb, var(--canvas) 88%, transparent);
  backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--line);
}
.site-header__inner {
  display: flex; align-items: center; gap: 24px; min-height: 76px;
}
.brand { display: flex; flex-direction: column; gap: 1px; text-decoration: none; flex: 0 0 auto; }
.brand__mark {
  font-family: "Fraunces", serif; font-weight: 600; font-size: 21px;
  letter-spacing: -0.01em; color: var(--ink);
}
.brand__sub {
  font-size: 10px; letter-spacing: 0.1em; text-transform: uppercase;
  color: var(--ink-muted); font-weight: 500;
}
.primary-nav { display: flex; gap: 28px; margin-inline-start: auto; }
.primary-nav a {
  position: relative; text-decoration: none; font-size: 13.5px; font-weight: 600;
  color: var(--ink-2); padding-block: 4px;
  transition: color var(--dur-fast) var(--ease-out);
}
.primary-nav a::after {
  content: ""; position: absolute; left: 0; right: 100%; bottom: 0; height: 2px;
  background: var(--accent-deep); transition: right var(--dur-med) var(--ease-out);
}
.primary-nav a:hover { color: var(--ink); }
.primary-nav a:hover::after { right: 0; }
.header-call {
  flex: 0 0 auto; text-decoration: none; font-weight: 700; font-size: 14px;
  color: var(--ink); border: 1.5px solid var(--ink); border-radius: var(--r-pill);
  padding: 9px 18px; white-space: nowrap;
  transition: background-color var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out);
}
.header-call:hover { background: var(--ink); color: var(--canvas); }

@media (max-width: 900px) {
  .site-header__inner { flex-wrap: wrap; padding-block: 12px; min-height: 0; }
  .header-call { margin-inline-start: auto; }
  .primary-nav {
    order: 3; width: 100%; margin-inline-start: 0; gap: 18px;
    overflow-x: auto; padding-block-end: 2px; scrollbar-width: none;
  }
  .primary-nav::-webkit-scrollbar { display: none; }
  .primary-nav a { white-space: nowrap; }
}
@media (max-width: 560px) { .brand__sub { display: none; } }

/* ---- Hero -------------------------------------------------------------------
   Full-bleed photograph, art-directed against the actual image: alternating
   line alignment, a real-feature annotation callout, hand-tuned cascade. */

.hero { position: relative; padding-block: clamp(20px, 3vw, 40px) var(--band-sm); }

.hero__frame { }

.hero__media {
  position: relative;
  aspect-ratio: 4 / 3;
  border-radius: var(--r-frame);
  overflow: hidden;
}
.hero__media img {
  position: absolute; inset: 0; width: 100%; height: 100%;
  object-fit: cover; object-position: 50% 42%;
  filter: saturate(0.92) contrast(1.04);
}
.hero__scrim {
  position: absolute; inset: 0;
  background:
    linear-gradient(90deg,
      rgba(10, 8, 6, 0.92) 0%,
      rgba(10, 8, 6, 0.90) 44%,
      rgba(10, 8, 6, 0.46) 57%,
      rgba(10, 8, 6, 0.14) 74%,
      rgba(10, 8, 6, 0.02) 100%);
}
.hero__scrim::after {
  /* extra bottom weight so the proof strip always sits on solid dark */
  content: ""; position: absolute; inset: 0;
  background: linear-gradient(0deg, rgba(10, 8, 6, 0.35) 0%, transparent 22%);
}

.hero__copy {
  position: absolute; left: 0; top: 0; bottom: 0; right: auto; z-index: 2;
  display: flex; flex-direction: column; justify-content: center;
  gap: 18px;
  width: min(58%, 680px);
  padding: clamp(20px, 4vw, 44px);
  color: var(--canvas);
}

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '261,560p' site.css && sed -n '561,820p' site.css && find img -maxdepth 2 -type f -print -exec file {} \\;" in /Users/zero-suminc./projects/one-box/spikes/wits-variants/c-artdirected/site
 succeeded in 0ms:
.hero__copy .eyebrow { color: rgba(250, 246, 239, 0.78); }
.hero__copy .eyebrow::before { background: var(--accent); }

.hero__title {
  font-size: clamp(30px, 4.4vw, 70px); line-height: var(--lh-tight);
  letter-spacing: var(--ls-tight); font-weight: 600; margin: 0;
  color: var(--canvas); white-space: nowrap;
}
.hero__title .line { display: block; }
.hero__title .line--r { text-align: right; }
.hero__title .ch { display: inline-block; opacity: 0; transform: translateY(0.4em); }
.hero__title.is-animating .ch {
  animation: charUp 0.55s var(--ease-out) forwards;
}
.hero__title.is-static .ch { opacity: 1; transform: none; }
.hero__title.is-animating .accent-word.word-fade {
  opacity: 0; transform: translateY(0.4em);
  animation: charUp 0.55s var(--ease-out) forwards;
}
.hero__title.is-static .accent-word.word-fade { opacity: 1; transform: none; }

@keyframes charUp {
  to { opacity: 1; transform: translateY(0); }
}

.hero__sub {
  font-size: var(--fs-lede); line-height: 1.55; color: rgba(250, 246, 239, 0.86);
  max-width: 44ch;
  opacity: 0; transform: translateY(14px);
  animation: fadeUp 0.7s var(--ease-out) forwards; animation-delay: 0.7s;
}
.hero__actions {
  display: flex; flex-wrap: wrap; gap: 12px;
  opacity: 0; transform: translateY(14px);
  animation: fadeUp 0.7s var(--ease-out) forwards; animation-delay: 0.9s;
}
.hero__actions .btn-ghost { border-color: rgba(250, 246, 239, 0.6); color: var(--canvas); }
.hero__actions .btn-ghost:hover { background: var(--canvas); color: var(--ink); border-color: var(--canvas); }

.hero__proof {
  display: flex; flex-wrap: wrap; gap: 10px; margin-block-start: 4px;
  opacity: 0; transform: translateY(14px);
  animation: fadeUp 0.7s var(--ease-out) forwards; animation-delay: 1.1s;
}
.hero__proof span {
  font-size: 12px; font-weight: 600; letter-spacing: 0.03em;
  color: rgba(250, 246, 239, 0.92);
  border: 1px solid rgba(250, 246, 239, 0.28);
  border-radius: var(--r-pill); padding: 7px 14px;
  background: rgba(250, 246, 239, 0.06);
}

@keyframes fadeUp {
  from { opacity: 0; transform: translateY(14px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ---- Hero annotation callout — pinned to the tester's display screen ----- */

.hero__callout { position: absolute; inset: 0; z-index: 3; pointer-events: none; }
.hero__callout-line {
  position: absolute; inset: 0; width: 100%; height: 100%;
  opacity: 0; animation: fadeIn 0.6s var(--ease-out) forwards; animation-delay: 1.3s;
}
.hero__callout-line line {
  stroke: rgba(250, 246, 239, 0.65); stroke-width: 0.35; stroke-dasharray: 1.4 1.2;
}
.hero__callout-dot {
  position: absolute; left: 71%; top: 80%; width: 12px; height: 12px;
  transform: translate(-50%, -50%);
  border-radius: 50%; background: var(--accent);
  box-shadow: 0 0 0 4px rgba(243, 91, 34, 0.28);
  opacity: 0; animation: dotIn 0.5s var(--ease-out) forwards; animation-delay: 1.15s;
}
.hero__callout-dot::after {
  content: ""; position: absolute; inset: -8px; border-radius: 50%;
  border: 1.5px solid rgba(243, 91, 34, 0.55); animation: ping 2.6s ease-out 2s infinite;
}
@keyframes dotIn { to { opacity: 1; } }
@keyframes fadeIn { to { opacity: 1; } }
@keyframes ping {
  0% { transform: scale(0.9); opacity: 0.7; }
  70%, 100% { transform: scale(1.9); opacity: 0; }
}

.hero__tip {
  position: absolute; left: 68%; top: 42%; width: min(220px, 27vw);
  background: rgba(20, 16, 12, 0.86);
  backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(250, 246, 239, 0.22);
  border-radius: 14px; padding: 14px 16px;
  pointer-events: auto;
  opacity: 0; transform: translateY(10px);
  animation: fadeUp 0.6s var(--ease-out) forwards; animation-delay: 1.35s;
}
.hero__tip-label {
  display: block; font-size: 10.5px; font-weight: 700; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--accent); margin-block-end: 6px;
}
.hero__tip p { font-size: 13px; line-height: 1.45; color: rgba(250, 246, 239, 0.92); margin: 0; }

@media (prefers-reduced-motion: reduce) {
  .hero__title .ch, .hero__title .accent-word.word-fade { opacity: 1 !important; transform: none !important; }
  .hero__sub, .hero__actions, .hero__proof, .hero__tip, .hero__callout-dot, .hero__callout-line {
    opacity: 1 !important; transform: none !important; animation: none !important;
  }
  .hero__callout-dot::after { animation: none !important; }
}

/* Mobile hero: text-over-photo is abandoned below 860px — a headline this
   long has no safe way to sit on top of a cropped photo on a small screen
   without risking overlap. Instead: copy stacks on a solid ink panel above
   a plain (uncropped-ratio) photo. Simpler, and impossible to overlap. */
@media (max-width: 860px) {
  .hero__media {
    aspect-ratio: auto; display: flex; flex-direction: column;
    border-radius: var(--r-frame); overflow: hidden;
  }
  .hero__scrim { display: none; }
  .hero__media img {
    position: static; order: 2; width: 100%; height: auto;
    aspect-ratio: 4 / 3; object-position: 50% 42%;
  }
  .hero__copy {
    position: static; order: 1; width: 100%; height: auto;
    background: var(--ink); gap: 16px;
    padding: clamp(28px, 7vw, 40px) clamp(20px, 6vw, 32px);
  }
  .hero__title { font-size: clamp(32px, 10vw, 52px); white-space: normal; }
  .hero__tip, .hero__callout-line, .hero__callout-dot, .hero__callout { display: none; }
  .hero__caption {
    display: block; order: 3;
    font-size: 12.5px; color: rgba(250, 246, 239, 0.78); background: var(--ink);
    border-top: 1px solid rgba(250, 246, 239, 0.2);
    padding: 14px clamp(20px, 6vw, 32px) clamp(24px, 6vw, 32px);
    margin: 0;
  }
}
@media (min-width: 861px) { .hero__caption { display: none; } }

/* ---- Reveal-on-scroll -------------------------------------------------------- */

.reveal {
  opacity: 0; transform: translateY(26px);
  transition: opacity 0.7s var(--ease-out), transform 0.7s var(--ease-out);
  transition-delay: calc(var(--i, 0) * 90ms);
}
.reveal.is-visible { opacity: 1; transform: translateY(0); }
@media (prefers-reduced-motion: reduce) {
  .reveal { opacity: 1; transform: none; transition: none; }
}

/* ---- Scope — die-cut cards + dense index ------------------------------------ */

.scope { padding-block: var(--band-lg); }

.scope__grid {
  display: grid; grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: var(--gap-md);
}

.scope-card {
  position: relative; background: var(--surface);
  border: 1px solid var(--line);
  padding: 30px 26px 26px;
  clip-path: polygon(0 0, calc(100% - 30px) 0, 100% 30px, 100% 100%, 0 100%);
  display: flex; flex-direction: column; gap: 12px;
  transition: transform var(--dur-med) var(--ease-out), box-shadow var(--dur-med) var(--ease-out);
}
.scope-card::before {
  content: ""; position: absolute; top: 0; right: 0; width: 30px; height: 30px;
  background: var(--accent-soft);
  clip-path: polygon(100% 0, 0 0, 100% 100%);
}
.scope-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 20px 40px -20px rgba(23, 19, 15, 0.28);
}
.scope-card__index {
  font-family: "Fraunces", serif; font-size: 15px; font-weight: 600;
  color: var(--ink-muted); letter-spacing: 0.02em;
}
.scope-card h3 {
  font-size: var(--fs-h3); font-weight: 600; letter-spacing: -0.01em;
  transition: color var(--dur-med) var(--ease-out);
}
.scope-card:hover h3 { color: var(--accent-deep); }
.scope-card p { font-size: var(--fs-body); color: var(--ink-muted); line-height: 1.6; }
.scope-card__arrow {
  margin-top: auto; display: inline-flex; align-items: center; gap: 6px;
  font-size: 13px; font-weight: 700; color: var(--ink);
  opacity: 0; transform: translateX(-6px);
  transition: opacity var(--dur-med) var(--ease-out), transform var(--dur-med) var(--ease-out);
}
.scope-card:hover .scope-card__arrow { opacity: 1; transform: translateX(0); }

.scope__also {
  margin-top: var(--gap-lg); padding-top: var(--gap-md);
  border-top: 1px solid var(--line);
  display: grid; grid-template-columns: 14ch minmax(0, 1fr); gap: var(--gap-md);
  align-items: start;
}
.scope__list { display: flex; flex-wrap: wrap; gap: 10px 20px; }
.scope__list li {
  font-size: 13.5px; color: var(--ink-muted); padding-block: 6px;
  position: relative; padding-left: 16px;
}
.scope__list li::before {
  content: ""; position: absolute; left: 0; top: 13px; width: 6px; height: 1.5px;
  background: var(--ink-muted);
}

@media (max-width: 900px) {
  .scope__grid { grid-template-columns: minmax(0, 1fr); }
  .scope__also { grid-template-columns: minmax(0, 1fr); gap: 10px; }
}

/* ---- Audience — two-tone icon-chip cards, horizontal anatomy --------------- */

.audience { padding-block: var(--band-md); background: var(--canvas-deep); }

.audience__grid {
  display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: var(--gap-md);
}
.audience-card {
  display: flex; flex-direction: column; gap: 14px;
  padding: 24px; background: var(--surface); border-radius: var(--r-card);
  border: 1px solid var(--line);
  transition: transform var(--dur-med) var(--ease-out), box-shadow var(--dur-med) var(--ease-out);
}
.audience-card:hover { transform: translateY(-5px); box-shadow: 0 18px 36px -22px rgba(23,19,15,.3); }
.audience-card__chip {
  width: 44px; height: 44px; border-radius: 12px;
  background: var(--accent-soft);
  display: flex; align-items: center; justify-content: center;
}
.audience-card__chip svg { width: 20px; height: 20px; stroke: var(--accent-deep); }
.audience-card h3 { font-size: 17px; font-weight: 600; transition: color var(--dur-med) var(--ease-out); }
.audience-card:hover h3 { color: var(--accent-deep); }
.audience-card p { font-size: 13.5px; color: var(--ink-muted); line-height: 1.55; }

@media (max-width: 900px) { .audience__grid { grid-template-columns: repeat(2, minmax(0,1fr)); } }
@media (max-width: 560px) { .audience__grid { grid-template-columns: minmax(0,1fr); } }

/* ---- Creed — full-bleed ink band, colour shift ------------------------------ */

.creed { background: var(--ink); color: var(--canvas); padding-block: var(--band-md); }
.creed__grid {
  display: grid; grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
  gap: var(--gap-lg); align-items: end;
}
.creed__statement {
  font-family: "Fraunces", serif; font-size: clamp(26px, 3.4vw, 42px);
  line-height: 1.2; font-weight: 500; letter-spacing: -0.01em;
  max-width: 22ch; margin-block-start: 12px;
}
.creed__rule { border: 0; border-top: 1px solid var(--line-on-ink); margin-block: 20px; }
.creed__note { font-size: var(--fs-lede); color: rgba(250, 246, 239, 0.78); max-width: 48ch; line-height: 1.65; }

@media (max-width: 860px) { .creed__grid { grid-template-columns: minmax(0, 1fr); } }

/* ---- Receive — contained offset panel -------------------------------------- */

.receive { padding-block: var(--band-lg); }
.receive__grid {
  display: grid; grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
  gap: var(--gap-lg); align-items: center;
}
.receive__list { display: flex; flex-direction: column; }
.receive-row {
  display: grid; grid-template-columns: 3.4ch minmax(0, 1fr); gap: 16px;
  padding-block: 20px; border-bottom: 1px solid var(--line);
}
.receive-row:first-child { border-top: 1px solid var(--line); }
.receive-row__n { font-family: "Fraunces", serif; font-weight: 600; color: var(--accent-deep); font-size: 15px; }
.receive-row h3 { font-size: 16.5px; font-weight: 600; margin-block-end: 4px; }
.receive-row p { font-size: 13.5px; color: var(--ink-muted); line-height: 1.55; }

.receive__panel { position: relative; }
.receive__panel-offset {
  position: absolute; inset: -18px -18px auto auto; width: 55%; height: 55%;
  background: var(--canvas-deep); border-radius: var(--r-frame); z-index: 0;
}
.receive__panel figure {
  position: relative; z-index: 1; margin: 0;
  border-radius: var(--r-frame); overflow: hidden;
  box-shadow: 0 30px 60px -30px rgba(23, 19, 15, 0.35);
}
.receive__panel img { aspect-ratio: 3 / 2; object-fit: cover; filter: saturate(0.9) contrast(1.03); }
.receive__panel figcaption {
  font-size: 12px; font-weight: 600; letter-spacing: 0.04em; text-transform: uppercase;
  color: var(--ink-muted); margin-top: 12px;
}

@media (max-width: 900px) {
  .receive__grid { grid-template-columns: minmax(0, 1fr); }
  .receive__panel-offset { display: none; }
}

/* ---- Quals — technical console ledger --------------------------------------- */

.quals { padding-block: var(--band-md); background: var(--ink); color: var(--canvas); }
.quals__grid {
  display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 1px;
  background: var(--line-on-ink); border: 1px solid var(--line-on-ink);
  border-radius: var(--r-card); overflow: hidden;
}
.quals-cell { background: var(--ink); padding: 24px; display: flex; flex-direction: column; gap: 8px; }
.quals-cell .eyebrow { color: rgba(250, 246, 239, 0.55); }
.quals-cell p { font-size: 13.5px; color: rgba(250, 246, 239, 0.82); line-height: 1.55; }
.quals .head h2 { color: var(--canvas); }
.quals .head .lede { color: rgba(250, 246, 239, 0.72); }

@media (max-width: 900px) { .quals__grid { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
@media (max-width: 560px) { .quals__grid { grid-template-columns: minmax(0, 1fr); } }

/* ---- Process — stepped timeline ---------------------------------------------- */

.process { padding-block: var(--band-lg); }
.steps { position: relative; display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: var(--gap-md); }
.steps::before {
  content: ""; position: absolute; top: 17px; left: 0; right: 0; height: 1px;
  background: var(--line); z-index: 0;
}
.step { position: relative; display: flex; flex-direction: column; gap: 10px; }
.step__n {
  position: relative; z-index: 1;
  width: 34px; height: 34px; border-radius: 50%;
  background: var(--surface); border: 1.5px solid var(--ink);
  display: flex; align-items: center; justify-content: center;
  font-family: "Fraunces", serif; font-weight: 600; font-size: 14px;
  transition: background-color var(--dur-med) var(--ease-out), color var(--dur-med) var(--ease-out), border-color var(--dur-med) var(--ease-out);
}
.step:hover .step__n { background: var(--accent); border-color: var(--accent); color: var(--ink); }
.step h3 { font-size: 17px; font-weight: 600; }
.step p { font-size: 13.5px; color: var(--ink-muted); line-height: 1.55; }

@media (max-width: 760px) {
  .steps { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: var(--gap-md) var(--gap-md); }
  .steps::before { display: none; }
}

/* ---- Work — full-bleed photo band with scrim -------------------------------- */

.work { position: relative; padding-block: var(--band-md); background: var(--ink); overflow: clip; }
.work .head { color: var(--canvas); }
.work .head .lede { color: rgba(250, 246, 239, 0.72); }
.work .eyebrow { color: rgba(250, 246, 239, 0.72); }

.work__strip {
  display: grid; grid-template-columns: 1.2fr 1fr 1fr; gap: 2px;
  margin-inline: calc(var(--edge-x) * -1);
}
.work-shot { position: relative; aspect-ratio: 4 / 3; overflow: hidden; margin: 0; }
.work-shot:first-child { aspect-ratio: 8 / 7; }
.work-shot img {
  width: 100%; height: 100%; object-fit: cover;
  filter: saturate(0.85) contrast(1.05);
  transition: transform 0.6s var(--ease-out);
}
.work-shot:hover img { transform: scale(1.05); }
.work-shot::after {
  content: ""; position: absolute; inset: 0;
  background: linear-gradient(0deg, rgba(10,8,6,.88) 0%, rgba(10,8,6,.15) 45%, transparent 70%);
}
.work-shot figcaption {
  position: absolute; left: 18px; right: 18px; bottom: 16px; z-index: 1;
  font-size: 12.5px; font-weight: 600; letter-spacing: 0.02em;
  color: rgba(250, 246, 239, 0.94);
}

@media (max-width: 760px) {
  .work__strip { grid-template-columns: minmax(0, 1fr); }
  .work-shot, .work-shot:first-child { aspect-ratio: 4 / 3; }
}

/* ---- About — offset portrait panel -------------------------------------------- */

.about { padding-block: var(--band-lg); }
.about__grid {
  display: grid; grid-template-columns: minmax(0, 0.66fr) minmax(0, 1.34fr);
  gap: var(--gap-lg); align-items: center;
}
.about__panel { position: relative; }
.about__panel-offset {
  position: absolute; inset: auto -16px -16px auto; width: 60%; height: 45%;
  background: var(--accent-soft); border-radius: var(--r-frame); z-index: 0;
}
.about__panel figure {
  position: relative; z-index: 1; margin: 0; border-radius: var(--r-frame); overflow: hidden;
  box-shadow: 0 30px 60px -28px rgba(23,19,15,.35);
}
.about__panel img { aspect-ratio: 3 / 4; object-fit: cover; filter: saturate(0.9) contrast(1.02); }
.about h2 { margin-block-end: 14px; }
.about .lede + .lede { margin-block-start: 14px; }
.about .lede { font-size: var(--fs-lede); color: var(--ink-2); max-width: 56ch; line-height: 1.65; }

@media (max-width: 860px) {
  .about__grid { grid-template-columns: minmax(0, 1fr); }
  .about__panel-offset { display: none; }
  .about__panel img { aspect-ratio: 4 / 3; }
}

/* ---- Coverage — data cards on a tinted band ------------------------------------ */

.coverage { padding-block: var(--band-md); background: var(--canvas-deep); }
.coverage__grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: var(--gap-md); }
.coverage-card {
  background: var(--ink); color: var(--canvas); border-radius: var(--r-card); padding: 30px;
}
.coverage-card .eyebrow { color: rgba(250, 246, 239, 0.55); margin-block-end: 12px; }
.coverage-card__primary {
  font-family: "Fraunces", serif; font-size: 24px; font-weight: 600; margin-block-end: 18px;
}
.coverage-card ul { display: flex; flex-direction: column; }
.coverage-card li {
  display: flex; justify-content: space-between; gap: 12px; font-size: 13.5px;
  padding-block: 11px; border-bottom: 1px solid var(--line-on-ink);
}
.coverage-card li:last-child { border-bottom: 0; }
.coverage-card li span:first-child { color: rgba(250, 246, 239, 0.92); }
.coverage-card li span:last-child { color: rgba(250, 246, 239, 0.55); }

@media (max-width: 760px) { .coverage__grid { grid-template-columns: minmax(0, 1fr); } }

/* ---- FAQ — accordion list, hairline rhythm ------------------------------------- */

.faq { padding-block: var(--band-lg); }
.faq__list { border-top: 1px solid var(--line); }
.faq-item { border-bottom: 1px solid var(--line); }
.faq-item summary {
  display: flex; align-items: center; justify-content: space-between; gap: 20px;
  padding-block: 22px; cursor: pointer; list-style: none;
  font-family: "Fraunces", serif; font-size: 18px; font-weight: 500; color: var(--ink);
  transition: color var(--dur-fast) var(--ease-out);
}
.faq-item summary::-webkit-details-marker { display: none; }
.faq-item summary:hover { color: var(--accent-deep); }
.faq-item__icon {
  flex: 0 0 auto; width: 26px; height: 26px; border-radius: 50%;
  border: 1.5px solid var(--ink); display: flex; align-items: center; justify-content: center;
  transition: transform var(--dur-med) var(--ease-out), background-color var(--dur-med) var(--ease-out), border-color var(--dur-med) var(--ease-out);
}
.faq-item[open] .faq-item__icon { transform: rotate(45deg); background: var(--accent); border-color: var(--accent); }
.faq-item__icon svg { width: 12px; height: 12px; stroke: var(--ink); }
.faq-item[open] .faq-item__icon svg { stroke: var(--ink); }
.faq-item p {
  font-size: var(--fs-body); color: var(--ink-muted); line-height: 1.65;
  max-width: 68ch; padding-block-end: 24px;
}

/* ---- Contact — full-bleed ink band ---------------------------------------------- */

.contact { background: var(--ink); color: var(--canvas); padding-block: var(--band-lg); }
.contact__grid { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); gap: var(--gap-lg); align-items: center; }
.contact h2 { font-size: var(--fs-h2); color: var(--canvas); line-height: 1.05; }
.contact__lede { color: rgba(250, 246, 239, 0.75); font-size: var(--fs-lede); max-width: 46ch; margin-block-start: 16px; line-height: 1.6; }
.contact__actions { margin-block-start: 26px; }
.contact__lines { display: flex; flex-direction: column; gap: 4px; }
.contact-line {
  display: flex; flex-direction: column; gap: 4px; padding-block: 18px;
  border-bottom: 1px solid var(--line-on-ink);
}
.contact-line .eyebrow { color: rgba(250, 246, 239, 0.55); }
.contact-line a {
  font-family: "Fraunces", serif; font-size: clamp(22px, 2.6vw, 30px); font-weight: 600;
  color: var(--canvas); text-decoration: none; overflow-wrap: anywhere;
  transition: color var(--dur-fast) var(--ease-out);
}
.contact-line a:hover { color: var(--accent); }

@media (max-width: 860px) { .contact__grid { grid-template-columns: minmax(0, 1fr); } }

/* ---- Footer ---------------------------------------------------------------------- */

.site-footer { padding-block: var(--band-sm); border-top: 1px solid var(--line); }
.site-footer__inner {
  display: flex; flex-wrap: wrap; justify-content: space-between; align-items: baseline;
  gap: 10px 20px; font-size: 12.5px; letter-spacing: 0.03em; color: var(--ink-muted);
}

/* ---- Reduced motion, global fallback ---------------------------------------------- */

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  *, *::before, *::after { animation-duration: 1ms !important; transition-duration: 1ms !important; }
}
img/hero.jpg
img/hero.jpg: JPEG image data, JFIF standard 1.02, resolution (DPI), density 72x72, segment length 16, progressive, precision 8, 1400x1050, components 3
img/ATTRIBUTION.md
img/ATTRIBUTION.md: Unicode text, UTF-8 text
img/rack.jpg
img/rack.jpg: JPEG image data, JFIF standard 1.02, resolution (DPI), density 72x72, segment length 16, progressive, precision 8, 1400x2100, components 3
img/copper.jpg
img/copper.jpg: JPEG image data, JFIF standard 1.01, resolution (DPI), density 72x72, segment length 16, progressive, precision 8, 1400x933, components 3
img/itroom.jpg
img/itroom.jpg: JPEG image data, JFIF standard 1.01, resolution (DPI), density 72x72, segment length 16, progressive, precision 8, 1400x933, components 3
img/office.jpg
img/office.jpg: JPEG image data, JFIF standard 1.01, resolution (DPI), density 72x72, segment length 16, progressive, precision 8, 1400x933, components 3
img/fiber.jpg
img/fiber.jpg: JPEG image data, JFIF standard 1.01, resolution (DPI), density 72x72, segment length 16, progressive, precision 8, 1400x937, components 3

codex
The implementation does ship substantially more craft than the D benchmark—staggered hero typography, scroll reveals, moving hover states, surface changes, and image-responsive composition—but the trust review is uncovering serious unsupported operational claims that the presentation masks.
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '1,220p' img/ATTRIBUTION.md && rg -n \"animation|transition|:hover|reveal|@keyframes|prefers-reduced-motion|555|\\.example|licensed|licensing|insured|certificate|certif|minimum|occupied|after-hours|test results|as-built|same (contact|person)|every run|normal\" index.html site.css site.js" in /Users/zero-suminc./projects/one-box/spikes/wits-variants/c-artdirected/site
 succeeded in 0ms:
# Image attribution — STOCK, NOT WITS'S OWN WORK

**These photographs are not WITS's work and must not ship.**

They are present so the layout, crops, aspect ratios and interactions can be
evaluated with real images in place. Devin authorised this explicitly on
2026-08-15 for prototype purposes, and will add a deployment guard that blocks
shipping a generated site containing third-party stock. That guard is out of
scope here.

Source: Unsplash (unsplash.com). Retrieved 2026-08-15 via crawl4ai search-page
scrape, then fetched from `images.unsplash.com`. The Unsplash licence permits
free commercial and non-commercial use without attribution; this file exists for
provenance, not because attribution is required.

| File | Unsplash photo ID | Used for |
|---|---|---|
| `hero.jpg` | `photo-1528845922818-cc5462be9a63` | Hero — fibre tester on a live switch |
| `fiber.jpg` | `photo-1594915440248-1e419eba6611` | What you receive — fibre patch panel |
| `copper.jpg` | `photo-1517373116369-9bdb8cdc9f62` | Gallery — copper patching |
| `itroom.jpg` | `photo-1580106815433-a5b1d1d53d85` | Gallery — equipment room aisle |
| `office.jpg` | `photo-1462826303086-329426d1aef5` | Gallery — commercial office interior |
| `rack.jpg` | `photo-1591808216268-ce0b82787efe` | About — rack detail |

## Selection rule applied

Per `DESIGN.md` §8, direction comes from Refero style `b11e1e78` (Ambrook):
*"candid, slightly desaturated photography … real people in natural or work
environments"*.

Deliberately **rejected** from the candidate set: every glowing-blue abstract
fibre-optic shot. That imagery is the dominant stock cliché for this trade and
contradicts the restraint of the design system — it signals "telecom brochure",
not "documented cable plant". Also rejected: a data-centre shot carrying a
visible third-party brand mark.

## Replacement brief

When WITS commissions its own photography:

> 16:9 and 4:3, colour. Candid and slightly desaturated. Real work in progress —
> a technician terminating a patch panel, a clean IDF with labelled runs, a rack
> mid-build. Natural light. No posed stock, no stage-lit server rooms, no
> imagery of another company's install. Shot on a WITS job site with the
> customer's written permission.

One photographable reference project is listed as an outstanding intake gap in
`CLAIMS.md`.
site.js:4: * 2. IntersectionObserver scroll reveals, once, staggered via --i.
site.js:5: * Both guarded by prefers-reduced-motion.
site.js:10:  var reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
site.js:41:              span.style.animationDelay = (base + count * step).toFixed(3) + "s";
site.js:52:              child.style.animationDelay = (base + count * step).toFixed(3) + "s";
site.js:70:  /* ---- Scroll reveals ---------------------------------------------------- */
site.js:72:  var revealTargets = document.querySelectorAll(".reveal");
site.js:75:    revealTargets.forEach(function (el) { el.classList.add("is-visible"); });
site.js:91:  revealTargets.forEach(function (el) { io.observe(el); });
site.css:101:  font-weight: 600; transition: top var(--dur-fast) var(--ease-out);
site.css:128:  font-style: normal;
site.css:144:  transition: transform var(--dur-med) var(--ease-out),
site.css:154:.btn-primary:hover {
site.css:162:.btn-ghost:hover { background: var(--ink); color: var(--canvas); transform: translateY(-2px); }
site.css:164:.btn-on-ink:hover { background: var(--canvas); border-color: var(--canvas); color: var(--ink); transform: translateY(-2px); }
site.css:190:  transition: color var(--dur-fast) var(--ease-out);
site.css:194:  background: var(--accent-deep); transition: right var(--dur-med) var(--ease-out);
site.css:196:.primary-nav a:hover { color: var(--ink); }
site.css:197:.primary-nav a:hover::after { right: 0; }
site.css:202:  transition: background-color var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out);
site.css:204:.header-call:hover { background: var(--ink); color: var(--canvas); }
site.css:273:  animation: charUp 0.55s var(--ease-out) forwards;
site.css:278:  animation: charUp 0.55s var(--ease-out) forwards;
site.css:282:@keyframes charUp {
site.css:290:  animation: fadeUp 0.7s var(--ease-out) forwards; animation-delay: 0.7s;
site.css:295:  animation: fadeUp 0.7s var(--ease-out) forwards; animation-delay: 0.9s;
site.css:298:.hero__actions .btn-ghost:hover { background: var(--canvas); color: var(--ink); border-color: var(--canvas); }
site.css:303:  animation: fadeUp 0.7s var(--ease-out) forwards; animation-delay: 1.1s;
site.css:313:@keyframes fadeUp {
site.css:323:  opacity: 0; animation: fadeIn 0.6s var(--ease-out) forwards; animation-delay: 1.3s;
site.css:333:  opacity: 0; animation: dotIn 0.5s var(--ease-out) forwards; animation-delay: 1.15s;
site.css:337:  border: 1.5px solid rgba(243, 91, 34, 0.55); animation: ping 2.6s ease-out 2s infinite;
site.css:339:@keyframes dotIn { to { opacity: 1; } }
site.css:340:@keyframes fadeIn { to { opacity: 1; } }
site.css:341:@keyframes ping {
site.css:354:  animation: fadeUp 0.6s var(--ease-out) forwards; animation-delay: 1.35s;
site.css:362:@media (prefers-reduced-motion: reduce) {
site.css:365:    opacity: 1 !important; transform: none !important; animation: none !important;
site.css:367:  .hero__callout-dot::after { animation: none !important; }
site.css:389:  .hero__title { font-size: clamp(32px, 10vw, 52px); white-space: normal; }
site.css:403:.reveal {
site.css:405:  transition: opacity 0.7s var(--ease-out), transform 0.7s var(--ease-out);
site.css:406:  transition-delay: calc(var(--i, 0) * 90ms);
site.css:408:.reveal.is-visible { opacity: 1; transform: translateY(0); }
site.css:409:@media (prefers-reduced-motion: reduce) {
site.css:410:  .reveal { opacity: 1; transform: none; transition: none; }
site.css:428:  transition: transform var(--dur-med) var(--ease-out), box-shadow var(--dur-med) var(--ease-out);
site.css:435:.scope-card:hover {
site.css:445:  transition: color var(--dur-med) var(--ease-out);
site.css:447:.scope-card:hover h3 { color: var(--accent-deep); }
site.css:453:  transition: opacity var(--dur-med) var(--ease-out), transform var(--dur-med) var(--ease-out);
site.css:455:.scope-card:hover .scope-card__arrow { opacity: 1; transform: translateX(0); }
site.css:489:  transition: transform var(--dur-med) var(--ease-out), box-shadow var(--dur-med) var(--ease-out);
site.css:491:.audience-card:hover { transform: translateY(-5px); box-shadow: 0 18px 36px -22px rgba(23,19,15,.3); }
site.css:498:.audience-card h3 { font-size: 17px; font-weight: 600; transition: color var(--dur-med) var(--ease-out); }
site.css:499:.audience-card:hover h3 { color: var(--accent-deep); }
site.css:592:  transition: background-color var(--dur-med) var(--ease-out), color var(--dur-med) var(--ease-out), border-color var(--dur-med) var(--ease-out);
site.css:594:.step:hover .step__n { background: var(--accent); border-color: var(--accent); color: var(--ink); }
site.css:619:  transition: transform 0.6s var(--ease-out);
site.css:621:.work-shot:hover img { transform: scale(1.05); }
site.css:695:  transition: color var(--dur-fast) var(--ease-out);
site.css:698:.faq-item summary:hover { color: var(--accent-deep); }
site.css:702:  transition: transform var(--dur-med) var(--ease-out), background-color var(--dur-med) var(--ease-out), border-color var(--dur-med) var(--ease-out);
site.css:728:  transition: color var(--dur-fast) var(--ease-out);
site.css:730:.contact-line a:hover { color: var(--accent); }
site.css:744:@media (prefers-reduced-motion: reduce) {
site.css:746:  *, *::before, *::after { animation-duration: 1ms !important; transition-duration: 1ms !important; }
index.html:16:    Phone   (407) 555-0142        — 555-01XX is the reserved fiction range
index.html:17:    Email   …@witscabling.example — .example is a reserved TLD
index.html:38:    <a class="header-call" href="tel:+14075550142">(407) 555-0142</a>
index.html:63:            field, fifteen running this business, and every run labeled and
index.html:67:            <a class="btn btn-primary" href="tel:+14075550142">Call for an estimate</a>
index.html:68:            <a class="btn btn-ghost" href="mailto:estimates@witscabling.example">Email us</a>
index.html:77:            certified before it&rsquo;s handed over &mdash; not just pulled
index.html:89:            <p>Every fiber run is tested and certified before it&rsquo;s handed over.</p>
index.html:98:    <div class="head reveal">
index.html:109:      <article class="scope-card reveal" style="--i:0">
index.html:119:      <article class="scope-card reveal" style="--i:1">
index.html:128:      <article class="scope-card reveal" style="--i:2">
index.html:155:      <div class="head reveal">
index.html:160:        <article class="audience-card reveal" style="--i:0">
index.html:167:        <article class="audience-card reveal" style="--i:1">
index.html:172:          <p>New suites, floor changes, and relocations &mdash; including work in buildings that stay occupied while it happens.</p>
index.html:174:        <article class="audience-card reveal" style="--i:2">
index.html:181:        <article class="audience-card reveal" style="--i:3">
index.html:193:  <section class="creed reveal">
index.html:207:          ends, test results you are handed, and a record of the plant that
index.html:215:  <section class="shell receive reveal" id="receive">
index.html:232:          <div><h3>An as-built record</h3><p>A plan of what was installed and where it terminates, that you keep.</p></div>
index.html:236:          <div><h3>One person to call</h3><p>The same contact through quoting, install and anything after.</p></div>
index.html:250:  <section class="quals reveal" id="quals">
index.html:268:          <p>Florida low-voltage licensing details supplied with the estimate.</p>
index.html:284:    <div class="head reveal">
index.html:289:      <article class="step reveal" style="--i:0">
index.html:294:      <article class="step reveal" style="--i:1">
index.html:299:      <article class="step reveal" style="--i:2">
index.html:304:      <article class="step reveal" style="--i:3">
index.html:313:  <section class="work reveal" id="work">
index.html:337:  <section class="shell about reveal" id="about">
index.html:364:  <section class="coverage reveal" id="coverage">
index.html:394:  <section class="shell faq reveal" id="faq">
index.html:418:          single suite is a different job from a floor in an occupied
index.html:435:        <summary>Can you work in an occupied building?
index.html:439:          Yes. Occupied floors, live suites and tenanted buildings are normal
index.html:440:          commercial work. Access windows, noise and after-hours requirements
index.html:445:        <summary>Is there a minimum job size?
index.html:449:          No fixed minimum. Small jobs and single-drop calls are worth the
index.html:455:        <summary>Do we get test results and a record of what was installed?
index.html:466:        <summary>Are you insured and licensed?
index.html:470:          Yes. A certificate of insurance can be issued to the general
index.html:471:          contractor or building owner on request, and licensing details are
index.html:481:          elsewhere in Florida and in Colorado is normal &mdash; ask, and we
index.html:498:          <a class="btn btn-on-ink" href="tel:+14075550142">Call (407) 555-0142</a>
index.html:504:          <a href="tel:+14075550142">(407) 555-0142</a>
index.html:508:          <a href="mailto:estimates@witscabling.example">estimates@witscabling.example</a>

codex
# Outside grade

## Per-dimension grades

| # | Dimension | Grade | Evidence (one sentence, cite a selector, file, or screenshot region) |
|---|-----------|-------|----------------------------------------------------------------------|
| 1 | Hero distinctiveness | B+ | The desktop hero layers image-specific annotation, alternating title alignment, proof chips, and staged copy, but `.hero__callout` disappears below 860px and leaves mobile with a conventional text-over-photo stack. |
| 2 | Color character | B | `site.css` establishes warm canvas, tinted, and dark surface tiers with disciplined orange accents, although the beige/ink/orange palette remains attractive rather than unmistakably WITS-specific. |
| 3 | Motion design | A- | `site.js` and `.reveal` provide letter-level hero choreography, staggered intersection reveals, moving hover states, a pulsing callout, shared timing tokens, and complete reduced-motion handling. |
| 4 | Section division & rhythm | B+ | The desktop capture alternates tinted panels, dark bands, timelines, full-bleed imagery, and offset media, but `.about` becomes cavernous on desktop and the mobile gallery-to-about sequence is visually prolonged. |
| 5 | Card craft | B | `.scope-card` adds a clipped corner, index, lift, shadow, and arrow reveal while `.audience-card` and `.coverage-card` provide variants, but several still resolve to bordered boxes and “More on this” is a non-interactive `<span>`. |
| 6 | Visual hierarchy | B+ | Consistent `.eyebrow`, committed Fraunces display type, restrained body copy, and strong dark/light inversions create clear reading order, though secondary copy becomes very small and recessive in the 1440px capture. |
| 7 | Variety / anti-monotony | A- | The page moves through card grids, a split manifesto, handover ledger, seam grid, timeline, photo strip, asymmetric portrait, coverage cards, and accordion without repeating consecutive topologies. |
| 8 | Balance & composition | B | The hero and `.receive__grid` distribute weight effectively, but the sparse process row and tall narrow `.about__panel` leave conspicuous unused space on desktop while mobile stacks several large image blocks consecutively. |
| 9 | Bespoke-ness | B | Cable-test annotation, labeled-handover narrative, technical imagery, and 15/30-year proof make this cabling-specific, but generic stock photography and unsupported operational copy prevent it from feeling authentically specific to WITS. |

## Overall grade

B

This is clearly above the D benchmark and contains a real visual and motion system, but its strongest art direction is desktop-only, several secondary sections fall back to polished template patterns, and the bespoke impression depends heavily on stock imagery and claims the brief never supplied.

## Ranked fixes

1. **Replace all stock photographs with commissioned WITS work and pin annotations to genuinely documented installation details.**  
   Hero distinctiveness B+ to A-, bespoke-ness B to A-, and trustworthiness materially improves.

2. **Remove or verify every invented licensing, insurance, minimum-job, occupied-building, testing, documentation, and service-policy claim.**  
   Bespoke-ness B to B+ by replacing synthetic specificity with defensible company proof; it also removes the launch-blocking trust failure.

3. **Retain an image-specific signature composition on mobile instead of hiding `.hero__callout` and reverting to a stacked panel.**  
   Hero distinctiveness B+ to A- and balance B to B+.

4. **Recompose `.about__grid` around a wider contextual image, owner portrait, or documented project artifact rather than the tall stock rack crop.**  
   Balance B to A- and bespoke-ness B to B+.

5. **Turn each `.scope-card` into a real destination or remove the false “More on this” affordance, then add cable-specific diagrams or deliverable previews.**  
   Card craft B to A- and bespoke-ness B to B+.

6. **Animate section internals independently—rows, image panels, timeline connectors, and captions—instead of revealing whole sections as one `.reveal` block.**  
   Motion design A- to A by extending the existing vocabulary into deliberate section choreography.

7. **Tighten the sparse desktop process/about spacing and break the mobile run of full-width imagery with captions, offsets, or horizontal scrolling.**  
   Section division B+ to A- and balance B to A-.

8. **Bind the palette to verified logo colors and apply one repeatable technical surface treatment across annotations, cards, and data panels.**  
   Color character B to A- and card craft B to B+.

## Trust check

The page ships fictional contact details—`(407) 555-0142` and `estimates@witscabling.example`—despite acknowledging them only in a source comment. `index.html` also invents Florida low-voltage licensing and insurance/COI status, “no fixed minimum,” occupied-building and after-hours practices, routine interstate travel, same-person continuity, and universal testing, certification, labeling, test-result, and as-built handover policies. Finally, all six images are stock photographs explicitly marked “must not ship” in `img/ATTRIBUTION.md`, yet visible copy such as “The work,” “On this cable,” and “Fiber termination at the panel” presents them as evidence of WITS’s own work.
tokens used
52,440
